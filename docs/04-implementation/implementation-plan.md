# Kế hoạch triển khai — Website Thiên Đức (PA2)
 
> **Trạng thái:** Tài liệu sống — nguồn sự thật cho tiến độ coding.
> **Nhóm:** 04 — Implementation · **Current as of: 2026-07-23**
> **Tài liệu liên quan:** [open-questions](../01-requirements/open-questions.md) · [deployment-guide](../07-deployment/deployment-guide.md) · [security](../05-security/README.md) · [audit-baseline](../08-audits-and-reports/current/2026-07-16-audit-baseline.md)
>
> Cấu trúc: §0 hiện trạng · §1 tóm tắt đã xong · §2 code đang chờ làm · §3 manual/go-live · §4 deferred · §5 backlog nội dung · §6 backlog tùy chọn · §7 changelog/audit trail.
> Quy ước trạng thái: `[ ]` chưa làm · `[~]` đang làm · `[x]` xong. Mã truy vết: `→n` (audit-baseline), `ED/YC/CL/KB-xx` (báo cáo gốc), `SEC-*` (security). Việc phụ thuộc công ty ghi **(chờ input câu n)**.

## Section 0 — Current status summary

- **Tính năng website + CMS: gần như hoàn tất.** FE public (Next.js/Vercel), Admin CMS (Vite), Backend (NestJS/Prisma/Postgres trên Render) đã chạy thật end-to-end; toàn bộ Admin đã nối API thật, song ngữ VI/EN cho nội dung production/audited đã xong. Chi tiết: **§1** + ledger **§7**.
- **Production-readiness: CHƯA hoàn tất — cố ý hoãn.** 🟠 **Cổng go-live BLOCKED/DEFERRED** (G7-M1, [note](../08-audits-and-reports/current/2026-07-19-g7-m1-manual-ops-verification.md)): hạ tầng còn Free/chưa xác nhận (backend + Postgres Free; backup/PITR + restore + monitoring chưa bật). Nâng plan trả phí + monitoring **bắt buộc trước go-live cuối**. Checklist: **§3**.
- **Số checkbox mở ≠ số việc code đang mở.** Nhiều `[ ]` mở là *manual ops / deferred / backlog*, không phải hàng đợi coding (di sản của lần chuyển văn xuôi→checkbox, xem [CHECKBOX-AUDIT-M1](#section-7--changelog--audit-trail)).
- **Việc code đang mở thật sự: 1** — **→6 Enforce CSP** (§2, **HOÃN** tới khi monitoring active). **Retire `POST /users` ĐÃ XONG** (CMS-RETIRE-DIRECT-USER-CREATE-M1, §7): route tạo tài khoản kèm mật khẩu đã gỡ hẳn, lời mời là lối cấp tài khoản duy nhất. **Forgot Password ĐÃ XONG** trọn luồng backend + Admin UI + a11y (CMS-AUTH-FORGOT-PASSWORD PHASE1/PHASE2/PHASE2B, §7) — chỉ còn UAT thủ công §3. Đợt hardening phân quyền/bảo mật 2026-07-22 đã đóng: R1 media-delete, R2 banner governance, R3 users read-only lock-in, TRUSTED-PROXY-RATE-LIMIT-FIX-M1, ADMIN-SPA-SECURITY-HEADERS-M1 (ledger §7). Coding/security còn lại đều deferred: CSP enforce (§2, chờ monitoring) + Auth HttpOnly (§4, post-launch). Các mục mở khác: manual/go-live (§3), deferred infra (§4), backlog tùy chọn (§6). Backlog nội dung (§5) phần code/CMS đã xong, chỉ chờ công ty nhập liệu.

## Section 1 — Completed work summary

> Tóm tắt theo workstream (không lặp từng phiên chat). Ledger chi tiết + mã truy vết ở **§7**.

- **1. Integrations / lead capture** — Form liên hệ `POST /contact` (rate-limit 5/IP/giờ, honeypot, validate) + email báo lead **Resend-only** chạy thật production; trang quản lý lead ở Admin. (→1, YC-09)
- **2. Content ops / CMS** — CRUD đầy đủ projects / news / pages / banners / cooperation + luồng nháp→duyệt→đăng; Admin 8+ trang nối API thật, `BilingualField` nhập VI/EN; seed pages/projects/news/**banners**. (→4, →11, ED-03/04/06/07, THIEN-DUC-BANNER-CONTENT-IMPLEMENTATION-M1)
- **3. Frontend UI/UX** — Đồng nhất bố cục dự án theo chuẩn Hưng Phú, carousel hạng mục, gallery có điều kiện, cơ cấu lại trang chủ↔giới thiệu chống trùng lặp, redesign typography (Playfair + Be Vietnam Pro), footer. (FRONTEND-UI-POLISH-M1, PROJECT-GALLERY-IMAGES-FIX-M1)
- **4. SEO / structured data / performance** — canonical + hreflang + sitemap/robots, JSON-LD Organization/NewsArticle/Breadcrumb, baseline đo production (Rich Results + Lighthouse), fix NO_FCP/NO_LCP. (→7, →13, YC-12)
- **5. Security / validation / roles** — 3 HIGH findings đóng (CORS, rate-limit, SQL-inj), HTTP security headers, `@MaxLength` toàn bộ DTO chữ, 3 cấp role, SUPER_ADMIN đăng ngay. (→3, SEC-*, ADMIN-SUPER-ADMIN-GLOBAL-APPROVAL-BYPASS-M1)
- **6. Deployment / monitoring docs** — `render.yaml` khai plan trả phí; runbook backup-restore / rollback / monitoring-and-alerting; Sentry cả 3 app (errors-only, no-op khi thiếu DSN). (→2, →5)
- **7. Testing / CI / process** — CI lint+build cả 3 repo + e2e backend; test FE (Jest) + Admin (Vitest); ENV cleanup, tách secret khỏi Git. (→8, SYS-P1-ADMIN-ROBUSTNESS, SMTP-REMOVAL-ENV-CLEANUP)
  - **E2E full-stack Playwright (THIEN-DUC-FULL-STACK-PLAYWRIGHT-E2E-M1, 2026-07-27)** — 82 test Playwright (Chromium) phủ lời mời/thiết lập/đăng nhập/quên-đặt lại mật khẩu/phân quyền/bảo mật quản lý tài khoản/form liên hệ/nội dung công khai/token privacy/accessibility/responsive; host tại repo admin, `webServer` tự dựng 3 server, DB `thien_duc_test` có cầu chì an toàn, transport email GIẢ (gate `NODE_ENV=test + MAIL_FAKE_TRANSPORT=1`), route hỗ trợ test chặn cứng localhost/`@e2e.test`. Thêm integration PostgreSQL thật (backend, 7 test: claim nguyên tử tương tranh, unique tokenHash, rollback, cascade, thu hồi refresh). Admin coverage (Vitest v8, baseline + ngưỡng chống hồi quy). CI: coverage cho admin, `prisma validate` + integration cho backend, workflow `e2e-fullstack.yml` mới. Chi tiết: [2026-07-27-fullstack-playwright-e2e](../08-audits-and-reports/current/2026-07-27-fullstack-playwright-e2e.md). CI cross-repo cần `WORKSPACE_TOKEN` (bằng chứng hiện tại là chạy cục bộ).
  - **A11Y-CONTRAST-M2 (2026-07-27)** — xử lý trọn vẹn color-contrast: darken `--color-brand` #b06613→#9f5a0f (cả admin + frontend) + đổi panel `bg-brand-soft`+`text-white`→`bg-brand`; **0 vi phạm axe color-contrast** trên admin (login/forgot/reset/setup/users) + frontend (home/contact/news + du-an/gioi-thieu/cong-ty) × 375/768/1280. Rule KHÔNG bị tắt; helper nay chặn đầy đủ; bộ Playwright 82→91 test. Viền focus vẫn ≥3:1 (5.33 trắng / 3.31 sidebar).
- **8. Media / Cloudinary / gallery** — Upload/optimize Cloudinary (WebP≤1200px, chống path-escape), hiển thị ảnh Cloudinary trên production, gallery lấy ảnh cấp dự án. (→ ED-05, PROJECT-IMAGES-UI-FIX-M1, PROJECT-GALLERY-IMAGES-FIX-M1)
- **Process** — Kiểm đếm & làm sạch checkbox của plan (IMPLEMENTATION-PLAN-CHECKBOX-AUDIT-M1, 2026-07-21).

## Section 2 — Active coding tasks

> Chỉ việc code actionable kế tiếp. Manual ops / backlog / ý tưởng tương lai **không** để ở đây.

- [ ] **(→6) Enforce CSP — bỏ Report-Only** · *Nhóm 5* · phiên: **SEC-CSP-ENFORCE-M1**
  - `frontend/next.config.ts` đang `Content-Security-Policy-Report-Only`, còn `unsafe-inline`/`unsafe-eval`.
  - Theo dõi report → chuyển sang `Content-Security-Policy`, thay `unsafe-*` bằng nonce/hash Next.js. Làm **sau** →5 (monitoring); khi enforce thêm domain ingest Sentry vào `connect-src`. Cập nhật `next.config.spec.ts` theo header mới.
  - **Trạng thái (SECURITY-CODING-REMAINING-AUDIT-M1, 2026-07-22): vẫn HOÃN** cho tới khi monitoring/Sentry DSN active (§3). Gồm cả CSP cho Admin SPA (đã có header cơ bản ở `admin/vercel.json`, còn CSP để pass sau). Chỉ prep (kiểm kê nguồn `script-src`/`connect-src`) là làm trước được.

- [x] **Forgot Password — trọn luồng (backend + Admin UI + a11y)** · *Nhóm 5* · phiên: **CMS-AUTH-FORGOT-PASSWORD-PHASE1-BACKEND-M1** + **PHASE2-ADMIN-UI-UX-M1** + **PHASE2B-ADMIN-A11Y-FIX-M1** (§7). Backend (3 endpoint công khai, bảng `PasswordResetToken`, email Resend, thu hồi phiên) và Admin UI (`/quen-mat-khau`, `/dat-lai-mat-khau`, link "Quên mật khẩu?", `AuthLayout` dùng chung, nút hiện/ẩn mật khẩu khả truy cập bàn phím) **đã xong**. Còn lại **chỉ là UAT thủ công production** (cần DB + Resend thật) → §3.
- [x] **Retire `POST /users` (tạo tài khoản kèm mật khẩu)** · *Nhóm 5* · phiên: **CMS-RETIRE-DIRECT-USER-CREATE-M1** — **ĐÃ XONG** (2026-07-31). Route + `CreateUserDto` + `UsersService.create()` đã gỡ hẳn; `POST /api/users` nay trả **404**. Lời mời (`POST /users/invitations`) là lối cấp tài khoản duy nhất. Chi tiết ở ledger §7.

## Section 3 — Manual production / go-live tasks

> Thao tác tay ngoài repo (Dashboard Render/Vercel/Sentry/UptimeRobot, DNS) — repo không làm thay được. **Go-live BLOCKED/DEFERRED cho tới khi xong.**

- [ ] **Render paid plan + backup/PITR + restore drill + rollback drill** — áp plan trả phí (`render.yaml`: web `starter` always-on, Postgres `basic-256mb`) + thanh toán khi sync Blueprint; nếu không nâng tại chỗ → di trú theo runbook **trước mốc 90 ngày**; xác nhận backup daily/retention/PITR; kiểm thử khôi phục 1 lần; diễn tập rollback 1 lần. Runbook: [backup-and-restore](../07-deployment/backup-and-restore.md) · [rollback-plan](../07-deployment/rollback-plan.md). (→2)
- [ ] **Sentry DSN + UptimeRobot monitors** — tạo 3 project Sentry + dán 3 DSN (backend `SENTRY_DSN` / frontend `NEXT_PUBLIC_SENTRY_DSN` / admin `VITE_SENTRY_DSN`) vào Render/Vercel rồi redeploy; tạo 2 monitor UptimeRobot (`/api` + trang chủ). Checklist: [monitoring-and-alerting](../07-deployment/monitoring-and-alerting.md). (→5)
- [ ] **ED-08 cron ngoài gọi `publish-scheduled`** — Render free ngủ sau 15′ nên `@Cron` nội bộ không đủ; đặt UptimeRobot/cron-job.org gọi `POST /api/news/publish-scheduled` (token ADMIN). Nếu đã always-on ở →2 thì cron nội bộ chạy — vẫn nên có cron ngoài.
- [ ] **DNS / HTTPS / domain chính thức** — **(chờ input câu 10)**.
- [x] **Áp migration production có chủ ý** — **COMPLETE**. Sau sự cố migration `20260731120000_search_unaccent`, production recovery đã hoàn tất và Prisma xác nhận *Database schema is up to date*. Toàn bộ **11 migration** hiện được ghi nhận là đã áp dụng, gồm `20260710120000_add_fulltext_search` và `20260711120000_add_cooperation_projects` vốn còn tồn đọng trước đó. **Không phải mọi lần áp đều trót lọt**: production đã có **một lần áp thất bại** (`P3009`) và chỉ trở lại sạch sau recovery có kiểm soát. **Bài học bắt buộc cho mọi migration production về sau**: backup kiểm chứng được → inspection read-only trước khi resolve → `migrate resolve` có kiểm soát → `migrate deploy` → xác minh sau deploy. Seed chỉ chạy lại khi cần, có chốt `SEED_CONFIRM_PRODUCTION=yes`. Xem [incident 2026-07-31](../08-audits-and-reports/current/2026-07-31-production-unaccent-migration-incident.md) · runbook [database-migrations](../07-deployment/database-migrations.md).
- [ ] **UAT thủ công** — Editor + Admin thật trên toàn luồng (đăng nhập, soạn bài, duyệt, form liên hệ, upload ảnh), gồm bấm tay `BannerFormDialog` / `PageFormDialog` / `BilingualField`.
- [ ] **Chạy checklist nghiệm thu go-live đầy đủ** (mục 3.3 báo cáo / mục 4 `ke-hoach-thien-duc.md`).
- [ ] **k6 / load test** 50–100 người đồng thời (CL-04/05) + Lighthouse staging (CL-02, SEO≥90 CL-10) + test khôi phục backup DB (CL-09).
- *Xác nhận nội dung hết placeholder (chờ input câu 1–8)* — con trỏ, không phải checkbox riêng; mục canonical ở **§5 "Xác nhận hết placeholder"** (gỡ slug khỏi `placeholderPaths` trong `seo.ts`).

## Section 4 — Deferred security / infrastructure

> Việc kỹ thuật tương lai, **không active**; phụ thuộc ngân sách / đánh đổi độ phức tạp. Không chặn go-live cơ bản.

- [ ] **(→9) Auth bằng cookie HttpOnly** · *Nhóm 5* — token đang ở localStorage (`admin/src/lib/api/client.ts`); HttpOnly loại rủi ro XSS lấy token (đổi lại phải xử lý CSRF). Việc lớn FE+BE. (Finding #10). **Trạng thái (SECURITY-CODING-REMAINING-AUDIT-M1, 2026-07-22): HOÃN sang post-launch** — scope luồng auth lớn (phát hành cookie + CSRF + refresh), rủi ro cao nếu làm sát production; Finding #10 mức LOW (trade-off SPA). Ưu tiên header cơ bản (đã xong ADMIN-SPA-SECURITY-HEADERS-M1) thay vì đổi luồng auth trước bàn giao.
- [ ] **(→10) Môi trường staging + WAF** · *Nhóm 7* — staging verify trước khi lên thật; WAF (Cloudflare trước API) chắn DDoS/scan. Phụ thuộc ngân sách.

## Section 5 — Content backlog / business input

> Phần **code/CMS đã sẵn sàng** — các mục dưới đánh `[x]` cho phạm vi kỹ thuật; việc còn lại là **công ty nhập liệu/duyệt nội dung**, **không chặn tiến độ code**. Đổ dữ liệu thật vào là trang tự hiện.

- [x] **Câu 4 — tuyển dụng / `openPositions`** · *Nhóm 2* — `data/careers.ts` + trang `tuyen-dung` dựng khung đầy đủ; `openPositions` **cố ý rỗng**, trang hiện "chưa có vị trí" + `noindex`. Đổ vị trí thật vào mảng là hiện.
- [x] **Câu 5 — chính sách NS / đào tạo / sơ đồ tổ chức** · *Nhóm 2* — 3 trang `dao-tao`, `chinh-sach-nhan-su`, `so-do-to-chuc-cong-ty` dựng khung + i18n (EN-SITE-WIDE F3), giữ `noindex`. Backlog nội dung tương lai.
- [x] **3 dự án seed/dev chờ publish** — `chung-cu-la-bonita`, `du-an-vung-tau` (Silver Sea Tower), `du-an-bay-hien` (Bảy Hiền Tower): chỉ trong `prisma/seed-projects.js`, **không có trong CMS production** (chỉ `khu-do-thi-hung-phu`). Nếu publish lên CMS phải nhập bản dịch EN **trước khi publish** (editor đã kiểm chứng — [ADMIN-ITEM-CONTENT-P2 M1](../08-audits-and-reports/current/2026-07-19-admin-item-content-p2-batch-m1.md)).
- [x] **Xác nhận hết placeholder** — các trang còn `noindex` (`tuyen-dung`, `dao-tao`, `chinh-sach-nhan-su`, `so-do-to-chuc-cong-ty`); gỡ slug khỏi `placeholderPaths` trong `seo.ts` khi có nội dung thật. Liên đới go-live: **§3**.
- [x] **Ảnh thật cho dự án** — cần ≥4 ảnh/dự án (upload qua Admin) để `ProjectPhotoStrip` chạy slide. Ảnh Vista Verde / Feliz en Vista / Hưng Phú Mall thuộc bản quyền CapitaLand/bên thứ ba — **công ty phải cung cấp**. Cần công ty duyệt nội dung pháp lý dự án trước go-live (câu 2).

## Section 6 — Optional backlog

> Nice-to-have / cải tiến tương lai. Không chặn go-live. Không nhầm với việc active. Nguồn chi tiết ở ledger §7.

- [x] **Tìm kiếm bỏ dấu (`unaccent`)** · *Nhóm 1/4* — **IMPLEMENTATION COMPLETE / PRODUCTION RECOVERY COMPLETE**. Repo sửa defect CSDL-sạch bằng schema qualification, chứng minh `migrate deploy` 11/11 từ DB rỗng, CI PostgreSQL 17 áp đủ 11/11 và E2E search-unaccent pass. Production **đã từng nhận một lần áp migration thất bại** (Prisma `P3009`) — tuyên bố trước đây rằng production chưa chạy migration là **sai**. Recovery đã hoàn tất qua cổng backup: backup production + checksum kiểm chứng, diễn tập restore thành công (17 bảng / 2 user / 18 bài tin), inspect failed row + partial object, rồi `migrate resolve --rolled-back` + deploy bản đã sửa. `migrate status` cuối: *Database schema is up to date*; cả hai index GIN tồn tại; hàm search dùng `public.immutable_unaccent`. Không `migrate reset`, không sửa tay `_prisma_migrations`. Bằng chứng recovery là **session-attested** (repo chưa lưu raw transcript) — theo dõi không chặn ở incident §5. Xem [incident 2026-07-31](../08-audits-and-reports/current/2026-07-31-production-unaccent-migration-incident.md). (YC-10)
- [ ] **Sentry source map upload** · *Nhóm 8* — **REPO PREPARATION COMPLETE** (frontend **và** Admin) · **EXTERNAL UPLOAD NOT VERIFIED**. Frontend: `withSentryConfig` với cổng đủ 3 biến (`SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT`), release theo commit SHA, xoá source map sau upload, upload hỏng **không** làm đổ build; 15 test (`src/lib/sentry-build.test.ts`). Admin: **hết BLOCKED** — đã cài `@sentry/vite-plugin@5.4.0`, cùng cổng 3 biến, release runtime **khớp** release upload qua `define`, source map chỉ sinh khi cổng bật (dạng `hidden`) rồi xoá sau upload; upload hỏng **làm đổ build** (cố ý khác frontend, lý do ở báo cáo); **29 test**. **Còn lại**: tạo token + đặt biến ở môi trường build + xác minh trên một deploy thật. Tách riêng khỏi →5. Báo cáo: [2026-08-02](../08-audits-and-reports/current/2026-08-02-optional-backlog-coding-completion.md).
- [ ] **Backup off-site tự động** · *Nhóm 7* — **REPO AUTOMATION COMPLETE** · **EXTERNAL STORAGE NOT CONFIGURED** · **PRODUCTION RESTORE DRILL NOT VERIFIED**. Bộ script ở `backend/scripts/backup/` + **adapter trung lập nhà cung cấp** (`BACKUP_UPLOAD_COMMAND`/`BACKUP_REMOTE_PREFIX`, có dry-run, lỗi upload lan truyền), **tên DB dùng-một-lần là duy nhất**, wrapper Windows Task Scheduler + xoay log, và **20 test an toàn chạy KHÔNG cần PostgreSQL** (`npm run test:backup`). Test bắt được một **defect thật**: `need_tool` chạy trước cầu chì nên máy thiếu `pg_dump` trả nhầm mã 4 thay vì mã 3 và cầu chì không kiểm chứng được — đã đổi thứ tự. **Còn lại**: chọn nhà cung cấp + credential + lần upload đầu + diễn tập khôi phục trên dữ liệu production; diễn tập dump/restore cục bộ vẫn chờ PostgreSQL client. Tách riêng khỏi →2. Báo cáo: [2026-08-02](../08-audits-and-reports/current/2026-08-02-optional-backlog-coding-completion.md).
- [ ] **G4 — tối ưu hiệu năng còn lại** · *Nhóm 4* — **PARTIALLY COMPLETE**. (a) LCP banner: **không sửa gì** — mọi tối ưu khuyến nghị đã có sẵn (`preload` chỉ slide đầu, `lazy` slide sau, `sizes=100vw`, `quality=90`); 4,3 s **nhiều khả năng** do cold start Render Free — **suy luận**, chưa đo tách bạch; đo lại sau khi nâng plan → EXTERNAL VERIFICATION REQUIRED. (b) TBT: **VẪN DEFERRED** — profiling cần build production **có dữ liệu thật** (backend + DB), máy phiên 2026-08-02 không có; quy trình 5 URL đã ghi sẵn để chạy khi có DB. (c) **Đo bundle: XONG** — `npm run analyze` dùng cờ Turbopack-native `--experimental-analyze` (đã đo: `@next/bundle-analyzer` là plugin webpack nên Turbopack **bỏ qua**, báo cáo rỗng → gỡ phụ thuộc); chunk lớn nhất **410,7 KB**, tổng JS client **1 033,9 KB / 24 file**. Kích thước **không phải** TBT. (d) **Hàng rào chống hồi quy: XONG** — 11 test khoá `quality={100}` (D8), preload chỉ slide đầu, slide sau vẫn lazy, allowlist `[75, 90]`, analyzer không chạy trong build thường; đã chứng minh test bắt lỗi. (e) `Organization`: 43 test hình dạng; validator ngoài **PENDING EXTERNAL CHECK**. (f) PSI/Lighthouse/Rich Results: **chưa chạy** — ngoài repo. (→13) Báo cáo: [2026-08-02](../08-audits-and-reports/current/2026-08-02-optional-backlog-coding-completion.md).
- [ ] **Schema.org mở rộng** (`RealEstateListing`/`Product`/`LocalBusiness`/`sameAs`) · *Nhóm 4* — **BLOCKED BY BUSINESS DATA** (không đổi; rà lại 2026-08-02, hàng rào còn nguyên và đúng chỗ, **không có khoảng trống thật** nên không sửa gì). Đã rà soát và lập bảng field còn thiếu cho từng loại trong báo cáo phiên; test hiện khoá luôn việc **cố ý không có** `sameAs`/`openingHours`/`geo`/`priceRange` để không ai vô tình thêm dữ liệu bịa. Chờ công ty mở bán (giá/offer), có `openingHours`/`geo`, hoặc cung cấp URL mạng xã hội. (→7)
- [x] **Test Zod schema 6 FormDialog (Admin)** · *Nhóm 8* — **COMPLETE**. Sáu schema (banner/cooperation/news/page/project/user) tách sang module riêng `*-schema.ts` (vừa hết cảnh báo `react-refresh/only-export-components`, vừa không kéo component vào mẫu số coverage) + **121 test** trực tiếp ở `src/components/form-schemas.test.ts`. Đồng thời **đóng khoảng lệch với backend DTO**: bổ sung trần 5.000 ký tự cho field song ngữ, 160 cho slug, 500 + hàng rào `isSafeImageRef`/`isSafeInternalPath` cho ảnh/href, 60 cho `objectPosition`, 120 cho `author`, 60 cho `categoryId`, định dạng ngày cho `eventDate`. (SYS-P1-ADMIN-ROBUSTNESS)

---

## Section 7 — Changelog / audit trail

> Ledger nén các việc đã hoàn tất, giữ mã truy vết + link báo cáo. **Không lặp mô tả dài đã tóm ở §1.** Dates chỉ giữ khi hữu ích cho lịch sử. Các mục con của một task được gộp thành một dòng.

### Phiên 2026-08-02 — Hoàn tất phần code của backlog §6 (THIEN-DUC-OPTIONAL-BACKLOG-CODING-COMPLETION-M2)

- [x] **THIEN-DUC-OPTIONAL-BACKLOG-CODING-COMPLETION-M2** — làm hết phần §6 **không cần database**; phần cần DB và phần ngoài repo **hoãn có chủ đích, ghi rõ**. Báo cáo: [2026-08-02-optional-backlog-coding-completion](../08-audits-and-reports/current/2026-08-02-optional-backlog-coding-completion.md).
  - **Admin Sentry source map — hết BLOCKED.** Cài `@sentry/vite-plugin@5.4.0`; logic cổng tách ra `src/lib/sentry-build.ts` (bản song sinh của frontend) nên test được **không cần build, không gọi mạng**. **Bất biến quan trọng nhất: release runtime KHỚP release upload** — `vite.config.ts` chèn `__SENTRY_RELEASE__` qua `define`, `main.tsx` đọc đúng biến đó, cả hai cùng sinh từ `resolveSentryRelease`. Source map **chỉ sinh khi cổng bật**, dạng `hidden` (không gắn `sourceMappingURL`), rồi plugin xoá khỏi `dist/` sau upload — đã đo bản build cổng-tắt: **0 file `.map`, 0 tham chiếu `sourceMappingURL`**. **Chính sách upload lỗi = ĐỔ BUILD**, cố ý khác frontend: cổng chỉ bật khi ta chủ động đặt đủ ba biến, nên upload hỏng nghĩa là release lên production **không có source map dùng được** — "thành công giả" còn tệ hơn đỏ ngay. **+29 test** (mọi tổ hợp thiếu biến, có/không SHA, plugin vắng mặt khi tắt và đúng một lần khi bật, token không lọt ra release/log).
  - **Backup — adapter TRUNG LẬP nhà cung cấp.** Dự án chưa chọn nhà cung cấp nên thêm `BACKUP_UPLOAD_COMMAND` + `BACKUP_REMOTE_PREFIX` + `BACKUP_UPLOAD_DRY_RUN` thay vì ràng vào AWS/B2/GCS; không đặt lệnh → giữ cục bộ, lệnh hỏng → **lan truyền** mã 7. Chỗ giữ chỗ thay bằng bash string replace (không `eval`/`sed` trên chuỗi người dùng). Tên DB kiểm chứng nay **duy nhất mỗi lần chạy** (`_<UTC>_<pid>_verify`, vẫn giữ đuôi `_verify` để cầu chì còn tác dụng). Thêm wrapper `scheduler-windows.ps1` cho Task Scheduler (nạp env không lộ ra dòng lệnh, tự **xoay vòng log**, lan truyền mã thoát).
  - **DEFECT THẬT do test mới bắt được — thứ tự cầu chì.** `need_tool` chạy **trước** các cầu chì an toàn, nên trên máy thiếu `pg_dump`/`pg_restore`: lời gọi trỏ vào **production** trả mã **4** ("thiếu lệnh") thay vì mã **3** ("đích không an toàn"), lỗi thiếu `DATABASE_URL` bị che, và **cầu chì không thể kiểm chứng được** nếu chưa cài PostgreSQL. Đổi thứ tự thành tham số → checksum → **cầu chì** → rồi mới kiểm công cụ. **+20 test chạy không cần DB** (`npm run test:backup`).
  - **G4 — đo bundle bằng cờ có sẵn, không thêm phụ thuộc.** Thử `@next/bundle-analyzer` và **đo được là nó không chạy**: Next 16 build bằng Turbopack nên plugin webpack bị bỏ qua, `.next/analyze/` **rỗng**; ép `--webpack` thì phân tích một bundle **khác** bundle production thật. → gỡ phụ thuộc, dùng `next build --experimental-analyze` qua `npm run analyze`. Wrapper chạy Next bằng `process.execPath` (không `shell: true` — nối chuỗi không escape, DEP0190; mà bỏ shell thì `npx.cmd` ném `EINVAL` trên Windows). Số đo: chunk lớn nhất **410,7 KB**, tổng JS client **1 033,9 KB / 24 file**. **TBT vẫn CHƯA profile** — cần build production có dữ liệu thật; quy trình 5 URL ghi sẵn.
  - **+11 hàng rào chống hồi quy hiệu năng** — không đo thời gian, không bám tên chunk/hash: khoá `quality={100}` (D8), preload chỉ `index === 0`, slide sau vẫn `lazy`, allowlist `[75, 90]`, analyzer không chạy trong `npm run build`. **Đã chứng minh test bắt lỗi**: đưa `quality={100}` trở lại → 3 đỏ; hoàn nguyên → xanh. Một defect **của chính helper test** bị bắt và sửa ngay: regex gỡ chú thích nuốt nhầm khối `images` vì chuỗi `pathname: "/ksnntvmu/**"` có chứa `/*`.
  - **Schema.org — rà xong, KHÔNG sửa.** Hàng rào "cố ý không có" `sameAs`/`openingHours`/`geo`/`priceRange` vẫn nguyên và đúng chỗ; không có khoảng trống thật. Không thêm loại nào, không bịa dữ liệu. Giữ **BLOCKED BY BUSINESS DATA**.
  - **Kiểm định**: Admin **306** test (+29) / coverage functions **48,20%** (ngưỡng 38) · Frontend **200** test (+11) · Backend unit **491 / 37 suite** (không sửa mã TS backend) · backup **20/20**. Lint: frontend 0/0, admin 0 lỗi + 1 cảnh báo có sẵn, backend 0 lỗi + **5 cảnh báo** (xem gạch đầu dòng cuối). `tsc` sạch cả 3 (frontend **phải build trước** vì Next 16 sinh kiểu `PageProps` vào `.next/types`). Mọi build xanh. `prisma validate` hợp lệ. **Không chạm database, không chạm production.**
  - **Hoãn có chủ đích (không phải thất bại)**: backend E2E, diễn tập dump/restore cục bộ, profiling TBT 5 trang, Playwright full-stack — đều cần DB mà máy phiên này không có (`psql`/`pg_dump` ngoài PATH, `.env` rỗng, không đoán credential). Upload Sentry thật, upload off-site, drill production, PSI/Lighthouse/Rich Results — ngoài repo.
  - **⚠ Phát hiện ngoài phạm vi → ĐÃ KHẮC PHỤC cùng phiên (được duyệt riêng).** Công backend của phiên CI 2026-07-31 (lint 5→0, healthcheck `-d`, Actions v5, `destroyStatusOf`, `loadInstrument`) **không tồn tại trong repo backend** — frontend và admin đã nhận commit, backend thì chưa (vẫn ở `28abb81`), nên con số "backend lint 0 cảnh báo / 492 test" trong tài liệu là **SAI so với repo** (thực tế 5 cảnh báo / 491 test). **Đã cài thật** trong reconciliation dưới đây; báo cáo CI 2026-07-31 và ma trận test-case đã gắn đính chính, **không viết lại lịch sử**.
  - **Reconciliation CI backend (phạm vi hẹp, chỉ backend)** — **lint 5 cảnh báo → 0 mà không tắt luật**: `cloudinary.uploader.destroy()` trả `any` → nhận `unknown` + type guard thật `destroyStatusOf()` (nay phân biệt "thất bại có lý do" với "không hiểu phản hồi", thay vì âm thầm báo `thất bại: undefined`); bốn matcher `expect.objectContaining`/`not.objectContaining`/`anything` trả `any` → ba hàm bọc trả `unknown`, hành vi lúc chạy không đổi. Lint tự bắt một assertion **thừa** do chính bản sửa tạo ra (`in` đã thu hẹp kiểu) — đã bỏ. **Healthcheck**: `pg_isready -U thienduc_ci` → thêm `-d thien_duc_test` (khớp `POSTGRES_DB` + `DATABASE_URL` cùng file); thiếu `-d` thì thăm dò DB trùng tên user, sinh `FATAL … does not exist` mỗi 5 giây và che mất lỗi DB thật. **Actions**: `checkout`/`setup-node` v4 → **v5** trong **một** workflow backend (mô tả cũ "4 workflow + upload-artifact" là tính gộp cả frontend/admin — backend không dùng `upload-artifact`). **Nhiễu `Thiếu SENTRY_DSN`**: sửa **hoàn toàn ở test**, `instrument.ts` không đổi một dòng — `loadInstrument()` đặt env, `resetModules`, thu `console.warn` vào **mảng** rồi trả lại ngay. Bản đầu trả spy còn sống thì spy ở thân `describe` chồng lên spy của test → cảnh báo **bị đếm hai lần**; dùng `jest.requireActual` thay `require` trần nên **không cần** `eslint-disable` nào. **Unit 491 → 492 do một test THẬT** (khẳng định cảnh báo thiếu DSN), không ép số. **Kiểm định backend**: lint **0/0** · `tsc` sạch · build xanh · unit **492/492, 37 suite**, hết nhiễu DSN · backup **20/20** · `prisma validate` hợp lệ. **E2E KHÔNG chạy** (không có `DATABASE_URL` dùng-một-lần được duyệt); không chạm 5432, không migration, không production.

### Phiên 2026-07-31 — Recovery migration unaccent trên production (THIEN-DUC-PRODUCTION-UNACCENT-MIGRATION-RECOVERY-M1)

- [x] **THIEN-DUC-PRODUCTION-UNACCENT-MIGRATION-RECOVERY-M1** — sự cố `P3009` trên production **đã đóng**, trạng thái **RESOLVED**. Báo cáo: [2026-07-31-production-unaccent-migration-incident](../08-audits-and-reports/current/2026-07-31-production-unaccent-migration-incident.md).
  - **Nguyên nhân gốc của sự cố quản trị**: khẳng định "migration chưa deploy ở đâu" được suy ra từ lịch sử commit chứ không phải từ `_prisma_migrations` của chính production — trong khi thực tế production **đã áp và thất bại một lần**. Mọi deploy sau đó dừng ở `P3009`.
  - **Cổng backup đã chặn đúng.** Phiên đầu dừng ở trạng thái `BLOCKED` vì không chứng minh được backup production usable. Việc dừng này là đúng: nó buộc phải tạo backup thật trước khi chạm vào lịch sử migration.
  - **Recovery có kiểm soát**: backup production thủ công + **checksum đã kiểm chứng** → **diễn tập restore thành công** trên PostgreSQL 18 cục bộ (**17 bảng / 2 user / 18 bài tin**) → inspect failed row + partial object → `prisma migrate resolve --rolled-back 20260731120000_search_unaccent` → deploy bản migration đã sửa → xác minh.
  - **Kết quả cuối**: `migrate status` = *Database schema is up to date*; `_prisma_migrations` giữ **cả hai** dấu vết trung thực (lần thất bại gốc `rolled_back_at` có giá trị / `applied_steps_count = 0`; lần thay thế `finished_at` có giá trị / `applied_steps_count = 1`); `public.projects_search_idx` + `public.news_posts_search_idx` đều tồn tại; hàm search dùng schema-qualified `public.immutable_unaccent(...)`. Đối chứng repo: CI PostgreSQL 17 áp **11/11** migration, E2E `search-unaccent` pass.
  - **Không** `migrate reset`, **không** xoá/sửa tay row `_prisma_migrations`, **không** `--applied` (chỉ `--rolled-back`, vì effect thiếu), **không** in credential/`DATABASE_URL`.
  - **Khoảng trống bằng chứng, ghi rõ**: các bước trên production là **session-attested** — repository **chưa** lưu raw transcript. Không làm sự cố quay lại blocked; hành động lưu trữ bản transcript đã sanitize là **theo dõi không chặn** (incident §5).
  - **Hệ quả tài liệu**: §3 "Áp migration production có chủ ý" chuyển sang `[x]` COMPLETE; §6 unaccent chuyển sang `[x]` IMPLEMENTATION COMPLETE / PRODUCTION RECOVERY COMPLETE.

### Phiên 2026-07-31 — Dọn nốt chất lượng CI (THIEN-DUC-CI-FLAKY-AND-WARNINGS-CLEANUP-M1)

- [x] **THIEN-DUC-CI-FLAKY-AND-WARNINGS-CLEANUP-M1** — đóng 4 test Playwright chập chờn + 5 cảnh báo lint + nhiễu log CI. Báo cáo: [2026-07-31-ci-flaky-and-warnings-cleanup](../08-audits-and-reports/current/2026-07-31-ci-flaky-and-warnings-cleanup.md).
  - > **⚠ ĐÍNH CHÍNH 2026-08-02** — **phần backend của phiên này chưa từng được commit.** Frontend và admin đã nhận đầy đủ; backend vẫn ở `28abb81` và **không** có: lint 5→0, `pg_isready -d`, Actions v5, `destroyStatusOf`, `loadInstrument`. Thực tế đo được ngày 2026-08-02: backend **0 lỗi / 5 cảnh báo**, unit **491**. Các mục backend đó **đã được cài thật** trong phiên `THIEN-DUC-OPTIONAL-BACKLOG-CODING-COMPLETION-M2` cùng ngày (xem mục kế tiếp) — từ thời điểm đó con số backend mới đúng: lint **0/0**, unit **492**. Các gạch đầu dòng backend bên dưới vì vậy mô tả **trạng thái đạt được ngày 2026-08-02**, không phải 2026-07-31.
  - **4 test chập chờn — nguyên nhân gốc là CỔNG HYDRATE, đã tái hiện 100% chứ không đoán.** Chạy lại trên máy dev **không** tái hiện được (file 26/26 xanh cả khi đã xoá sạch `.next`; toàn bộ 187 test xanh với `--retries=0`). Tái hiện tất định bằng CDP `Emulation.setCPUThrottlingRate` **20×** trong một file repro tạm (đã xoá sau khi lấy bằng chứng): **đúng 4 test đó đỏ, không thừa test nào**; thêm cổng hydrate thì **4/4 xanh**. Làm chậm MẠNG không tái hiện được vì `page.goto` mặc định đã chờ `load` — cửa sổ nguy hiểm nằm **sau** `load`, lúc JS còn đang chạy và React chưa gắn handler. Hệ quả đo được: nút slider bấm vào khoảng trống thì không handler nào nhận (thẻ đứng yên → `expect.poll` hết hạn 10s); link `next/link` bấm đúng lúc giao thời thì điều hướng không xảy ra hoặc **cùng cú bấm bị xử lý hai lần** → hai mục lịch sử cho cùng `?page=2` → một lần `goBack()` không quay về được trang 1.
  - **Bản sửa — chỉ ở tầng test, KHÔNG thêm thuộc tính test nào vào mã production.** Helper mới `e2e/helpers/hydration.ts` chờ `<next-route-announcer>` — phần tử App Router tự chèn vào `document.body` trong `useEffect` của `AppRouterAnnouncer`, đã kiểm chứng **không có** trong HTML server render. Kèm theo: chờ **đúng** URL lấy từ `href` thật của thẻ (`waitUntil: 'commit'` vì điều hướng client không phát `load` mới), chốt `aria-current` của trang hiện tại **trước** khi bấm và **trước** khi `goBack()`, thêm khẳng định `previous` `toBeEnabled` sau khi bấm next. Cùng cổng hydrate được thêm cho 4 test cùng lớp rủi ro tuy chưa từng đỏ. **Không** `waitForTimeout`, **không** tăng `retries`, **không** bỏ/nới test nào. Một defect do chính bản sửa gây ra đã bị bắt ở lần xác minh đầu và sửa ngay: so chuỗi URL nguyên không khớp vì link phân trang có kèm neo `#danh-sach-tin` → đổi sang so `pathname` + `searchParams`.
  - **Lint backend 5 cảnh báo → 0.** `cloudinary.uploader.destroy()` trả `any` → nhận `unknown` + type guard thật (shape lạ nay ném lỗi thay vì âm thầm so `undefined !== 'ok'`); `expect.objectContaining`/`anything` trả `any` → ba hàm bọc trả `unknown`. Không tắt luật, không `eslint-disable` diện rộng.
  - **Healthcheck Postgres CI**: `pg_isready -U thienduc_ci` → thêm `-d thien_duc_test`. Thiếu `-d` thì pg_isready thăm dò database trùng tên user (không tồn tại) nên cứ 5 giây lại sinh `FATAL: database "thienduc_ci" does not exist`; container vẫn "healthy" nên nhiễu này che mất lỗi DB thật.
  - **Log duplicate-key trong E2E = LOẠI A, CÓ CHỦ ĐÍCH** (`news_posts_slug_key`, `account_invitations_token_hash_key`, `password_reset_tokens_token_hash_key`) — cả ba do test đường-âm cố ý ghi trùng để khẳng định ràng buộc unique. Giữ nguyên ràng buộc + khẳng định; **không** suppress vì dòng log do chính PostgreSQL ghi, tắt nó phải hạ `log_min_messages` và sẽ giấu luôn lỗi DB thật.
  - **GitHub Actions**: `checkout`/`setup-node`/`upload-artifact` v4 (Node 20 đã bỏ) → **v5** (Node 24) ở cả 4 workflow. Cố ý **không** nhảy v6/v7 (đổi cách lưu credential; chặn checkout fork PR) vì không có lợi ích tương xứng. Không đặt `ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION`.
  - **CI tùy chọn**: cache **chỉ** `.next/cache` (không bao giờ cache cả `.next` — đó là kết quả build); nhiễu `Thiếu SENTRY_DSN` trong unit test nay thành **khẳng định** (`jest.resetModules` + spy `console.warn` + `require`), hành vi production không đổi.
  - **Phụ thuộc**: admin 16→**15**, frontend 33→**30** bằng `npm audit fix` (chỉ bản vá patch). Backend **giữ nguyên 6** — đã đo hai đường và cả hai đều tệ hơn: `npm audit fix` kéo `prisma` 7.8→7.9.1 (+46 gói) mà **không** nâng `@prisma/client` (Prisma đòi hai bên cùng phiên bản), còn `npm update brace-expansion fast-uri` làm **6 → 29** advisory nên đã hoàn nguyên lockfile + `npm ci`. Không advisory backend nào chạm runtime đã deploy. **Không** dùng `npm audit fix --force`. Rủi ro tồn đọng chạm runtime **duy nhất**: `sharp`/libvips ở frontend — npm chỉ đề xuất `next@9.3.3` (hạ cấp), phải chờ bản Next kế tiếp.
  - **Một lỗi MỚI xuất hiện đúng một lần, KHÔNG tái hiện được** — `responsive.e2e.ts` @ 1280x800 (trang đăng nhập Admin trắng trơn) đỏ ở lần chạy toàn bộ đầu tiên sau khi sửa (186/1). **Không** nằm trong 4 test của phiên. Điều tra: ảnh chụp trắng; không có trace (config `trace: 'on-first-retry'` + `--retries=0`); log server không có lỗi Vite/Admin; test kế tiếp dùng đúng app đó xanh sau 1,2 giây; chạy lại file **3 lần = 41 pass mỗi lần**; probe tạm 40 vòng `goto('/dang-nhap')` có bắt console/pageerror/request hỏng/response ≥400 → **0/40 lần trắng**. Kết luận: **một lần duy nhất, không tái hiện**; KHÔNG sửa mò, KHÔNG tăng retries, KHÔNG thêm `waitForTimeout`. Đề xuất (chưa làm): đổi `trace` sang `retain-on-failure` để lần sau còn dữ liệu chẩn đoán.
  - **Kiểm định**: backend unit **492** (+1) · backend e2e **96** · migration **11/11** · frontend jest **189** · admin vitest **277** (coverage functions 47,41%) · Playwright **187 pass / 0 fail / 0 flaky / 0 skip** (`--retries=0`, 8,3 phút — lần chạy lại sau khi điều tra xong) + file đã sửa chạy **5 lần liên tiếp, 5/5 × 26 pass** + `responsive.e2e.ts` **3/3 × 41 pass**. Lint 0 lỗi **0 cảnh báo** cả 3 repo; `tsc` sạch; build xanh. Chỉ chạm `127.0.0.1:5432/thien_duc_test`; không chạm production.

### Phiên 2026-07-31 — Backlog tùy chọn §6, phần làm được trong repo (THIEN-DUC-OPTIONAL-BACKLOG-REPO-WORK-M1)

- [x] **THIEN-DUC-OPTIONAL-BACKLOG-REPO-WORK-M1** — làm hết phần §6 có thể cài + kiểm chứng **trong repo**, phần phụ thuộc bên ngoài chỉ chuẩn bị chứ KHÔNG tuyên bố đã xong. Báo cáo: [2026-07-31-optional-backlog-repo-work](../08-audits-and-reports/current/2026-07-31-optional-backlog-repo-work.md).
  - **Tìm kiếm bỏ dấu — checkbox `[x]` SAI, tính năng chưa từng tồn tại.** `grep unaccent` toàn backend = 0 kết quả, không có mục ledger nào, và đo trực tiếp trên Postgres: `"du an"` KHÔNG khớp `"Dự án"`. Đã cài thật: migration `20260731120000_search_unaccent` — `CREATE EXTENSION IF NOT EXISTS unaccent`, hàm bọc `immutable_unaccent` (BẮT BUỘC: `unaccent(text)` một tham số là STABLE nên không dùng được trong biểu thức index; dạng hai tham số thì được), `DROP INDEX` **trước** khi đổi thân hàm rồi dựng lại (không làm vậy sẽ có index lệch dữ liệu → kết quả sai âm thầm), và bọc từ khoá ở `search.service.ts` (bọc một phía là vô nghĩa). **Bẫy đã tránh**: index tin tức tên `news_posts_search_idx` chứ không phải `news_search_idx` — bản nháp đầu drop nhầm tên. +23 test integration.
  - **Test Zod 6 FormDialog** — tách schema sang `*-schema.ts` (không export từ file component: vừa hết cảnh báo `react-refresh/only-export-components`, vừa tránh kéo component vào mẫu số coverage — đo được nếu import từ component thì `functions` tụt 45,76% → 35,53%, dưới ngưỡng 38). **+121 test**. Đóng khoảng lệch Admin↔backend DTO: mọi khác biệt được sửa đều là *Admin lỏng hơn backend*; khác biệt CÓ CHỦ Ý (EN không bắt buộc, `en: ""`, zero-width space) được ghi nhận và **không** đụng tới.
  - **Sentry source map** — `withSentryConfig` (tích hợp chính thức, không thêm dependency, không script tự chế) với cổng đủ 3 biến, release theo commit SHA, `deleteSourcemapsAfterUpload`, upload hỏng không làm đổ build. Logic cổng tách ra `src/lib/sentry-build.ts` + **15 test** chạy không cần mạng/token. Đã kiểm build **không có token** vẫn xanh. **KHÔNG upload thật.**
  - **Backup off-site** — bộ 4 script + mẫu env + mẫu lịch chạy (chưa kích hoạt). Chạy thật trên `thien_duc_test`: dump 195 KB + checksum → restore vào DB dùng-một-lần ra **17 bảng / 2 user / 4 project**; 6 đường lỗi đều đúng mã thoát (2/3/3/3/6) và **0 lần rò mật khẩu**. `.gitignore` chặn dump + file env. **KHÔNG upload đi đâu, KHÔNG diễn tập trên production.**
  - **M2-R2 reduced motion — chẩn đoán lại**: nguyên nhân là **specificity** chứ không phải thứ tự stylesheet (`.stagger-sides.is-revealed > *:nth-child(odd)` = 0,3,0 thắng luật trung hoà 0,2,0). Sửa bằng cách lặp đúng hình dạng selector trong khối media — không `!important`, không tắt animation toàn cục, không đụng `:hover`. **Đã chứng minh test bắt lỗi**: hoàn nguyên CSS → 2 đỏ; áp lại → 6/6 xanh.
  - **D8 `quality={100}` là BUG THẬT, không phải lệch cấu hình** — đo được `/_next/image?q=100` trả **HTTP 400** (`"q" parameter (quality) of 100 is not allowed`) vì `images.qualities: [75,90]`, tức ảnh bản đồ **hỏng trên mọi trang chi tiết dự án**. Đổi sang `quality={90}` (55,2 KB vs 32,7 KB ở q=75, w=640).
  - **Schema.org** — +43 test hình dạng cho `Organization`/`NewsArticle` (JSON hợp lệ sau `serializeJsonLd`, `@id` ổn định giữa vi/en, URL tuyệt đối, không giá trị giữ chỗ, chống breakout). Các loại mở rộng vẫn **BLOCKED BY BUSINESS DATA** — đã lập bảng field còn thiếu.
  - **Sửa kèm — rò rỉ fixture của backend e2e (defect có sẵn).** `http-foundation.e2e-spec.ts` chỉ xoá những slug được `push` thủ công nên mỗi lần chạy để lại vài bài **PUBLISHED**; một trong số đó mang ảnh `res.cloudinary.com/demo/...` (host không có trong `images.remotePatterns`) khiến trang công khai trả **500** và làm **31 test Playwright đỏ**. Đã đổi sang dọn theo **không gian tên slug** qua Prisma ở cả `beforeAll` lẫn `afterAll` — dọn được cả rác của lần chạy trước. Sau khi sửa: **187/187 Playwright xanh**.
  - **CI bắt defect migration trên CSDL SẠCH — đã sửa.** Bản đầu chạy được ở máy dev nhưng đổ ở CI: `P3018 / 42883 function immutable_unaccent(text) does not exist … SQL function "project_search_document" during inlining`. **Nguyên nhân**: hàm `LANGUAGE sql` lưu thân dưới dạng VĂN BẢN, tên bên trong chỉ phân giải lúc **inlining** theo `search_path` của phiên gọi — mà thân `*_search_document` gọi `immutable_unaccent(...)` KHÔNG qualify. Bất đối xứng trong lỗi (hàm ngoài tìm thấy, hàm trong không) là vì tên trong câu lệnh phân giải lúc parse, còn tên trong thân phân giải muộn lúc dựng index — thời điểm PostgreSQL 17 chạy với `search_path` bị siết. **KHÔNG phải do rác CSDL cục bộ**: đã dựng lại `thien_duc_test` rỗng hoàn toàn và bản CŨ vẫn qua trên PostgreSQL 16 — thứ che khuyết điểm là khác biệt môi trường. Đã tái hiện cơ chế cục bộ trên chính đối tượng thật (không qualify + `search_path=pg_catalog,pg_temp` → đúng lỗi của CI; có qualify → chạy được với cả `search_path=''`). **Sửa thẳng vào migration chưa deploy ở đâu, không thêm migration vá**: `WITH SCHEMA public` cho extension, `public.immutable_unaccent` tạo trước mọi hàm phụ thuộc, mọi lời gọi + định nghĩa + biểu thức index đều qualify, giữ đúng tên index thật. Chứng minh lại từ CSDL rỗng: **11/11 migration, 0 lỗi**, `_prisma_migrations` ghi `finished=true / rolled_back=false`. KHÔNG dùng `migrate resolve`, KHÔNG đánh dấu applied bằng tay. CI vốn đã dựng `postgres:17` mới mỗi lần và chạy `migrate deploy` trước khi test — chính nó bắt được lỗi này nên **không thêm hạ tầng mới**.
  - **Chưa làm, nói rõ**: TBT trang dự án chưa profile; Lighthouse/PSI chưa chạy; `@sentry/vite-plugin` cho Admin chưa cài (cần duyệt dependency).
  - **Kiểm định**: backend unit 491 · backend e2e **96** (+23) · Admin vitest **277** (+121, coverage functions 47,41% ≥ 38) · Frontend jest **189** (+43 schema.org, +15 cổng Sentry) · Playwright **187 pass / 0 fail / 0 flaky / 0 skip** (`--retries=0`). `tsc` sạch cả 3 repo; lint 0 lỗi (backend 5 + admin 1 warning **có sẵn**, frontend 0/0); mọi build xanh. DB sau tất cả: 0 fixture sót, đúng 2 tài khoản seed. Chỉ chạm `localhost:5432/thien_duc_test`; không upload, không gửi mail, không chạm production.

### Phiên 2026-07-31 — Gỡ hẳn đường tạo tài khoản trực tiếp (CMS-RETIRE-DIRECT-USER-CREATE-M1)

- [x] **CMS-RETIRE-DIRECT-USER-CREATE-M1** — cấp tài khoản nay **chỉ còn một đường: lời mời**.
  - **Hành vi cũ**: `POST /api/users` (SUPER_ADMIN-only) nhận `CreateUserDto` **có field `password`**, `UsersService.create()` bcrypt rồi ghi thẳng `passwordHash`. Admin UI đã bỏ dùng từ PHASE3B/3C nhưng route vẫn **reachable**; client Admin còn `usersApi.createUser()` + hook `useCreateUser()` chết (không màn hình nào gọi).
  - **Hành vi mới**: route **gỡ hẳn**, không stub, không 410 — `POST /api/users` trả **404** cho mọi vai trò, kể cả SUPER_ADMIN đã đăng nhập, và **không ghi hàng nào** vào DB. Tạo tài khoản qua `POST /users/invitations`; người được mời tự đặt mật khẩu ở `/thiet-lap-tai-khoan`.
  - **Lý do bảo mật**: xoá nốt khả năng một quản trị viên **biết mật khẩu của người khác**. Sau PHASE3C (`PATCH /users/:id` đã bỏ `password`), `POST /users` là lối cuối cùng còn lại; giữ nó vừa là bề mặt tấn công vừa phá bất biến "không ai đặt mật khẩu hộ".
  - **Backend**: gỡ handler `create()` ở `users.controller.ts`, `UsersService.create()` (kèm nhánh bcrypt), **xoá file** `dto/create-user.dto.ts`. `UpdateUserDto` trước kế thừa `PartialType(OmitType(CreateUserDto, ['password']))` → nay **khai báo tường minh** `email?/name?/role?/isActive?`, hợp đồng `PATCH /users/:id` **không đổi** (đã có sẵn spec DTO chứng minh). Giữ nguyên: lời mời, accept-invitation, forgot/reset password, login, refresh, seed/bootstrap, fixture `/api/test/*` (vẫn gate `NODE_ENV=test + MAIL_FAKE_TRANSPORT=1` + `TestOnlyGuard` localhost, ẩn khỏi Swagger). **Không đổi schema, không migration** — `User.passwordHash` vẫn cần cho đăng nhập.
  - **Admin**: gỡ `createUser()` + `CreateUserInput` (`lib/api/users.ts`) và hook `useCreateUser()` (`lib/api/queries.ts`) — đều là code chết. Không màn hình nào đổi hành vi.
  - **Test thêm**: backend `test/retired-user-create.e2e-spec.ts` (12 test trên Postgres thật: 404 cho khách/SUPER_ADMIN, payload `password`/`passwordHash`/rỗng đều 0 hàng DB, tổng số user không đổi sau 5 lần thử, lời mời vẫn tạo đúng 1 tài khoản pending, lời mời từ chối `password` 400, ADMIN/EDITOR 403, khách 401); unit `users.controller.spec.ts` + `users.service.spec.ts` (không còn handler/method `create`, không còn route `POST ""`); Admin vitest `lib/api/users.retired-create.test.ts` (5 test, kiểm ở mức mã nguồn để không kéo module chưa test vào mẫu số coverage); Playwright `e2e/admin/retired-user-create.e2e.ts` (6 test, gồm bắt request: UI tạo tài khoản không phát sinh `POST /users` nào).
  - **Kết quả kiểm thử**: backend unit **491 pass**, backend e2e/integration **73 pass** (4 suite), Admin vitest **156 pass** (coverage functions 45.76% ≥ ngưỡng 38%), Frontend jest **131 pass**, Playwright full-stack **181 pass / 0 fail / 0 flaky / 0 skip** (chạy `--retries=0`). `tsc --noEmit` + `lint` + `build` sạch ở cả 3 repo. DB `thien_duc_test` sau toàn bộ: **0 fixture `@e2e.test` sót**, đúng 2 tài khoản seed, `passwordHash`/`setupCompletedAt` không đổi.
  - **Tương thích ngược**: quét toàn workspace (Admin, Frontend, script, CI, tài liệu, seed, Playwright helper) — **không consumer thật nào** còn gọi `POST /users`. Không cần migration dữ liệu, không cần thông báo cho người dùng cuối.
  - **Release note**: *Quản trị viên không còn tạo tài khoản kèm mật khẩu. Thêm người dùng bằng "Thêm tài khoản" → hệ thống gửi email lời mời → người được mời tự đặt mật khẩu. Người quên mật khẩu dùng "Quên mật khẩu?".*

### Phiên 2026-07-30 — Siết chặt trước bàn giao (FINAL-RELEASE-HARDENING-M2)

- [x] **FINAL-RELEASE-HARDENING-M2** — đóng 7 khoảng trống ưu tiên cao của M1. Kết luận vẫn **🟠 PARTIALLY GREEN** nhưng đã thu hẹp nhiều. Báo cáo: [2026-07-30-m2-release-hardening](../08-audits-and-reports/current/2026-07-30-m2-release-hardening.md).
  - **XSS-01 (Critical) — TÌM ĐƯỢC VÀ ĐÓNG MỘT XSS LƯU TRỮ THẬT.** JSON-LD nhúng bằng `JSON.stringify` không escape `<`, mà schema có chứa chữ do CMS nhập (`headline`/`description`/`name`). **Đã tái hiện**: tiêu đề `BREAKOUT</script><img src=x onerror=alert(1)>` (EDITOR+ tạo được) đóng sớm thẻ script, `<img>`/`<svg>` thành phần tử HTML **sống** trên trang công khai. Sửa bằng `frontend/src/lib/json-ld.ts` (`serializeJsonLd` escape `<`,`>`,`&`,U+2028/9 sang `\uXXXX` — vẫn JSON hợp lệ, structured data không đổi nghĩa), dùng chung cho `json-ld.tsx` + `breadcrumb.tsx`. **Xác minh đã đóng**: khối `NewsArticle` parse được, headline round-trip nguyên văn, không còn payload thô trong DOM.
  - **URL-01 (High) — hàng rào URL phía SERVER.** Trước đó `href`/`image`/`url`/`gallery[]` chỉ có `@IsString() @MaxLength()`; đo được **9/9 biến thể nguy hiểm đều được nhận (201)**. React 19 tình cờ chặn `javascript:` lúc render nhưng đó là cơ chế client và không chặn `data:`/`vbscript:`/URL ngoài. Thêm `common/validators/safe-url.ts` — **allowlist theo hình dạng** (`@IsSafeInternalPath` cho href; `@IsSafeImageRef` cho ảnh: nội bộ hoặc `https://`), bỏ ký tự điều khiển/khoảng trắng Unicode trước khi soi nên `java<TAB>script:` trượt ngay. Áp cho **7 DTO**. Dữ liệu thật trong DB đều qua được (đã truy vấn kiểm).
  - **Hợp đồng nội dung = VĂN BẢN THUẦN** (editor `Input`/`Textarea`, không dependency rich-text/sanitizer, frontend render `<p>{text}</p>`). Vì vậy **cố ý KHÔNG** thêm sanitizer allowlist HTML — sẽ ngụ ý HTML được hỗ trợ và phá nội dung hợp lệ như `a < b`.
  - **D5 — chuỗi bắt buộc chỉ gồm khoảng trắng bị chặn.** `@IsNotBlank` dùng chung; hợp đồng **TỪ CHỐI, KHÔNG TỰ TRIM** (nên `@MaxLength` vẫn đo trên chuỗi thô — có test). Áp cho `TranslatedTextDto.vi`, `LongTranslatedTextDto.vi`, các `slug`, và contact `name`/`phone`/`message`. **Không** áp cho field optional (`en?` vẫn nhận rỗng).
  - **D6 — trần `content[]` = 500 đoạn**, căn cứ dữ liệu thật: 18 bài nhập từ website hiện hữu có min 2 / trung vị 17 / p90 41 / **cao nhất 48** đoạn ⇒ 500 là ~10× dư địa. Trần tổng payload vẫn là body parser 2 MB. Áp đồng nhất backend + **schema Zod của Admin**.
  - **D10 — build không còn phụ thuộc backend còn thức.** `staticParamsSafe` + `isApiReachableAtBuild` (cổng duy nhất ở `[locale]/layout.tsx`) + cờ `BUILD_REQUIRE_API=1`. **Đã kiểm đủ 4 chế độ**; chế độ "có URL + backend ngủ" từ **exit 1** (`Failed to collect page data`) thành **exit 0** + cảnh báo kèm `ECONNREFUSED`, render on-demand. Rủi ro thật vì backend ở Render Free ngủ sau 15′. `frontend/ci.yml` nay ghi tường minh `NEXT_PUBLIC_API_URL: ''`.
  - **Next 16.2.6 → 16.2.12** (chỉ một dòng `package.json`, không nâng gì khác, không `audit fix --force`). **Toàn bộ 9 advisory trực tiếp của Next biến mất** (gồm *middleware/proxy bypass App Router single locale* trùng đúng `src/proxy.ts`). Còn lại chỉ 2 phụ thuộc **bắc cầu** do Next ghim: `postcss`, `sharp` — **không sửa được** (npm chỉ đề xuất hạ Next về 9.x) → tồn đọng **M2-R1**.
  - **Responsive đủ 8 viewport** (thêm 320/360/390/1024/1440) + trang mới (tin tức list/chi tiết, dự án list/chi tiết/hạng mục, giới thiệu, công ty thành viên, Admin tổng quan/tin tức/dự án/banner/liên hệ + modal form). **41/41 xanh.** Việc mở rộng **phát hiện 1 defect CSS thật**: `ProjectItemGallery` thiếu `min-w-0` → dải ảnh chìa ra **440px ở viewport 320** và tràn ở mọi bề rộng < 1440 (sửa bằng `min-w-0`, **không** dùng `overflow-x:hidden` toàn cục).
  - **Đổi cơ sở đo tràn ngang** (`e2e/helpers/layout.ts`): probe trong trình duyệt cho thấy trạng thái **trước reveal** cố ý lệch `translateX(±128px)` và `documentElement.scrollWidth` **không co lại** sau khi phần tử đã về trong khung (danh sách phần tử vượt khung RỖNG mà scrollWidth vẫn 1128). Nay đưa trang về trạng thái đã reveal một cách tất định rồi **khẳng định theo phần tử** (loại phần tử bị cắt bởi khối bên trong — cắt có chủ đích). Chặt hơn, không nới lỏng.
  - **Flake `forgot-reset`: 13 lượt liên tiếp `--retries=0` → 13/13 xanh** ⇒ xếp loại **flake lịch sử, không tái hiện được** (tổng 16 lượt tính cả M1). Không thêm sleep, không nới timeout, không bật retry.
  - **Test mới: +129** (backend unit 394→**489**, backend E2E 40→**61**, admin 146→**151**, frontend 123→**131**). Playwright 139→**175**.
  - **Kiểm định**: cả 3 repo `tsc` sạch · lint **0 lỗi** (backend còn đúng 8 warning CŨ, admin 1 warning cũ, frontend 0/0) · mọi build xanh · `prisma validate`/`generate` xanh · coverage admin **43,27%** lines (ngưỡng 34) **PASS**, không hạ ngưỡng.
  - **Phát hiện phụ chưa sửa (M2-R2, Low–Med)**: `prefers-reduced-motion` **không** trung hoà hiệu ứng reveal trên trang `.projects-motion` (thứ tự stylesheet) ⇒ người chọn giảm chuyển động vẫn thấy trượt.
  - **An toàn**: chỉ localhost/127.0.0.1 · DB `thien_duc_test` · mail giả · không Resend/Cloudinary/Sentry · không chạm DB production · không in secret · **không commit, không push**.

### Phiên 2026-07-30 — Sửa môi trường E2E backend trong CI (BE-E2E-CI-ENV-FIX-M1)

- [x] **BE-E2E-CI-ENV-FIX-M1** — Job `e2e` của `backend/.github/workflows/ci.yml` đỏ **45/61** test, toàn bộ ở `test/http-foundation.e2e-spec.ts` với cùng một lỗi `beforeAll`: `Thiếu SUPER_ADMIN_EMAIL/SUPER_ADMIN_PASSWORD`. **Một lỗi bootstrap môi trường**, không phải 45 defect sản phẩm.
  - **Nguyên nhân** — bộ E2E cần **hai** tài khoản bootstrap (`ADMIN_EMAIL` vai trò ADMIN cho `app.e2e-spec.ts`; `SUPER_ADMIN_EMAIL` vai trò SUPER_ADMIN cho `http-foundation.e2e-spec.ts`), nhưng job chỉ khai `ADMIN_*` và seed bằng `npm run prisma:seed` (`prisma/seed.js` — chỉ gieo được **một** tài khoản). `prisma/seed-e2e.js` (gieo cả hai) lại **không thể chạy** trong job vì cầu chì an toàn đòi database đúng tên `thien_duc_test`, còn service container đặt `thien_duc_e2e`.
  - **Sửa** — DB CI đổi `thien_duc_e2e` → **`thien_duc_test`** (khớp cầu chì + khớp `admin/.github/workflows/e2e-fullstack.yml`); thêm `SUPER_ADMIN_EMAIL`/`SUPER_ADMIN_PASSWORD`, `NODE_ENV=test`, `MAIL_FAKE_TRANSPORT=1` ở **cấp job** nên seed / preflight / verify / `test:e2e` dùng **cùng một** bộ giá trị; seed đổi sang `npx prisma db seed` (= `seed-e2e.js`). Mật khẩu test lấy từ secret `E2E_SUPER_ADMIN_PASSWORD` / `E2E_ADMIN_PASSWORD` nếu repo đã cấu hình, chưa có thì dùng giá trị giả dùng-một-lần → CI tất định. Không dùng secret production.
  - **Preflight an toàn mới** — `prisma/preflight-e2e.js` (`npm run e2e:preflight`, chạy ngay sau `npm ci`): chỉ kiểm **sự có mặt** biến + host cục bộ + tên DB `thien_duc_test` + `NODE_ENV=test` + `MAIL_FAKE_TRANSPORT=1` + độ dài mật khẩu + hai email phải khác nhau. In **tên biến, không in giá trị** (không email/mật khẩu/DATABASE_URL). `verify-bootstrap.js` nay kiểm **cả hai** tài khoản (vai trò + `setup_completed` + `is_active`) thay vì chỉ một.
  - **Không nới lỏng gì** — không bỏ một assertion fail-fast nào, không skip test, không `continue-on-error`, không `|| true`.
  - **Kiểm định** (cục bộ, đúng hợp đồng env như CI): `prisma validate` · `generate` · `migrate deploy` · `db seed` · `verify:bootstrap` (ADMIN + SUPER_ADMIN đúng vai trò) xanh. `test:e2e` **61/61** (0 skip); `http-foundation.e2e-spec.ts` chạy riêng **45/45 × 3 lượt**. `jest 489/489` · `tsc` sạch · `lint` 0 lỗi (8 warning **cũ**, không thuộc thay đổi này) · `build` xanh. Chỉ localhost + DB `thien_duc_test`, email transport GIẢ, không chạm DB/dịch vụ production, không commit/push.

### Phiên 2026-07-30 — Audit chất lượng website cuối (THIEN-DUC-FINAL-WEBSITE-QUALITY-AUDIT-M1)

- [x] **THIEN-DUC-FINAL-WEBSITE-QUALITY-AUDIT-M1** — audit tự động toàn bộ chức năng trước bàn giao, dựng lại inventory + ma trận test-case **từ mã nguồn thật** (không kế thừa kết luận báo cáo trước). Kết luận: **🟠 PARTIALLY GREEN** (không phải FULLY GREEN). Báo cáo: [2026-07-30-final-website-quality-audit](../08-audits-and-reports/current/2026-07-30-final-website-quality-audit.md) · ma trận: [2026-07-30-final-test-case-matrix](../08-audits-and-reports/current/2026-07-30-final-test-case-matrix.md).
  - **2 defect SẢN PHẨM (đã sửa + test hồi quy)**:
    - **D1 (High)** — vượt trần body JSON trả **500** thay vì **413**. `PayloadTooLargeError` của body-parser không phải `HttpException` nên rơi vào nhánh 500 chung, lại còn bắn Sentry cho một lỗi phía client — trái với chính ý định ghi ở `common/body-limit.ts`. Sửa `http-exception.filter.ts`: lỗi middleware mang `status`/`statusCode` **4xx** trả đúng mã đó + thông điệp an toàn + `details: null` + không báo Sentry; **chỉ nhận 4xx** nên lỗi 5xx thật vẫn đi đường 500.
    - **D2 (High)** — thiếu field song ngữ bắt buộc trả **500** (`PrismaClientValidationError`) thay vì **400**. `@ValidateNested()` một mình không chặn `undefined`. Ban đầu tưởng chỉ ảnh hưởng `news`, nhưng khi gửi payload đủ mọi scalar bắt buộc thì **`projects`, `pages`, `banners` cũng 500** ⇒ khuyết điểm hệ thống. Thêm `@IsDefined()` cho **14 field trên 7 DTO** (+21 dòng, 0 dòng xóa). Có test khẳng định `Update*Dto` (`PartialType`) **không** bị siết theo → PATCH từng phần vẫn hợp lệ.
  - **2 defect TEST (đã sửa)**:
    - **D3 (High)** — `admin/e2e/public/news-slider-pagination.e2e.ts` khẳng định `login.status === 200` trong `beforeAll` (đúng là **201**, Nest mặc định cho POST) → `beforeAll` đổ, **26 test bị bỏ chạy âm thầm** trong khi tổng kết vẫn đọc "112 passed".
    - **D4 (High)** — lộ ra **sau khi** sửa D3: test bàn phím slider tin `press()` vào `<div role="group">` **không có `tabindex`** ⇒ không focus được, tiêu điểm rơi về `BODY`, phím không nổi bọt tới handler → đỏ **6/6 lượt (tất định, KHÔNG phải flaky)**. Probe trong trình duyệt chứng minh **sản phẩm không lỗi**: focus nút next thật rồi ArrowRight thì slide đổi. Sửa test theo đúng khuôn test banner (focus control thật trước khi bấm). 26/26 × 5 lượt xanh.
  - **+59 test mới**: `http-exception.filter.spec.ts` (10), `common/dto/required-nested-text.spec.ts` (25), `test/http-foundation.e2e-spec.ts` (24 — integration PostgreSQL thật, phủ JSON dị dạng / content-type lạ / 413 / whitelist / enum / 404 / 409 / không lộ stack / DRAFT+PENDING không lộ public).
  - **Kiểm định**: Backend `jest 394/394` · `test:e2e 40/40` · `tsc` sạch · `lint` 0 lỗi (8 warning **cũ**, số lượng không đổi trước/sau) · `build` xanh. Admin `vitest 146/146` · coverage vượt mọi ngưỡng (43,28% lines vs ngưỡng 34 — **không hạ ngưỡng**) · `tsc` sạch · `lint` 0 lỗi · `build` xanh. Frontend `jest 123/123` · `tsc` sạch · `lint` 0 lỗi/0 warning. Playwright: **113 → 139 test**.
  - **Chưa đạt FULLY GREEN vì**: (a) còn **1 quan sát chập chờn chưa giải thích được** (`forgot-reset` "mật khẩu quá ngắn"); (b) **không có bằng chứng CI xanh** (chạy cục bộ, không push); (c) **27 case chưa có test**, đáng lo nhất là **sanitize rich content + URL scheme độc hại (D7)**; (d) **Lighthouse không chạy**, 5/8 viewport chưa kiểm, axe chưa phủ nhiều route; (e) **`next@16.2.6` còn 9 advisory (3 High)**, một cái là *middleware/proxy bypass App Router single locale* — trùng đúng kiến trúc `src/proxy.ts`; bản sửa **16.2.12** (patch) **chưa áp**.
  - **Defect còn mở, cố ý KHÔNG tự sửa** (cần quyết định chủ dự án): **D5** tiêu đề rỗng/chỉ khoảng trắng vẫn tạo được (thay đổi quy tắc nghiệp vụ); **D6** thiếu `@ArrayMaxSize` cho `content[]`; **D7** khoảng trống test sanitize/URL scheme; **D8** `images.qualities` lệch (`quality={100}` vs `[75,90]`); **D9** 8 lint warning tồn đọng + script `lint` của backend có `--fix` (tự ghi file).
  - **An toàn**: chỉ `localhost`/`127.0.0.1`; DB `thien_duc_test` (cầu chì 2 tầng); transport email GIẢ, không email thật; không Resend/Cloudinary/Sentry/Gmail; không chạm DB production; không in secret/token; **không commit, không push**; không skip test, không tắt rule axe, không nới timeout, không thêm sleep, không `overflow-x:hidden` toàn cục.
  - ⚠️ **Bài học đo lường**: lượt Playwright đầu chạy qua `| tail` nên **exit code bị `tail` che** (báo 0 dù suite đỏ) — phải đọc dòng tổng kết, không tin exit code của pipeline. Và phân loại "flaky" chỉ được kết luận **sau khi chạy lặp**: D4 thoạt trông như flake nhưng thực ra đỏ 6/6.

### Phiên 2026-07-29 — Slider tin trang chủ + phân trang /tin-tuc (NEWS-SLIDER-AND-PAGINATION-M1)

- [~] **THIEN-DUC-NEWS-SLIDER-AND-PAGINATION-M1** — Trang chủ đổi 3 thẻ tin tĩnh thành **slider** (mobile 1 / tablet 2 / desktop 3 thẻ, trượt 1 thẻ/lần, **không autoplay**), viết theo pattern `ProjectItemsCarousel` sẵn có — **không thêm thư viện**. `/tin-tuc` có **phân trang thật, 9 bài/trang**, trang hiện tại nằm trong URL (`?page=2`); `?page=` sai/0/âm/chữ/1 → redirect URL sạch, vượt trang cuối → redirect về trang cuối có thật. Backend `GET /news` thêm `page`/`limit` **tùy chọn** (giữ mảng phẳng khi không có → không breaking), trần `limit` 50, chỉ PUBLISHED, sắp **tất định** `publishedAt desc, id desc` (khoá phụ `id` sửa lỗi thứ tự không tất định của bài trùng `publishedAt`), `count`+`findMany` trong một transaction. Canonical mỗi trang tự trỏ chính nó (không noindex). **54 unit test mới** xanh (BE 10 / FE 44). ⚠️ **22 spec Playwright + axe đã viết nhưng CHƯA chạy** — thiếu `.env` cục bộ ở cả 3 repo nên backend không boot được; chưa có bằng chứng đo được cho a11y/overflow/Back-Forward. Chi tiết + cách chạy: [news-slider-and-pagination-m1](../08-audits-and-reports/current/2026-07-29-news-slider-and-pagination-m1.md).

### Phiên 2026-07-29 — Nâng trần nội dung dài (LONG-FORM-CONTENT-LIMIT-M1)

- [x] **LONG-FORM-CONTENT-LIMIT-M1** — Biên tập viên không lưu được bài dài: API trả `400 content.0.vi must be shorter than or equal to 5000 characters`.
  - **Nguyên nhân** — `content[]` của tin tức và trang tĩnh dùng chung `TranslatedTextDto` (trần **5.000** ký tự, đặt ở →3 cho các field *ngắn*). Admin CMS chỉ tách đoạn theo **dòng trống**, nên dán cả bài không có dòng trống thì toàn bộ nội dung rơi vào `content[0].vi` và vượt trần. Trần là ràng buộc chống payload khổng lồ, **không** phải giới hạn biên tập — nên nó chặn nhầm nội dung hợp lệ.
  - **Trần cũ → mới: 5.000 → 100.000 ký tự/đoạn**, chỉ cho nội dung dài. Thêm `common/dto/long-translated-text.dto.ts` (`LongTranslatedTextDto`, `MAX_LONG_TEXT_LENGTH`) dùng ở `CreatePageDto.content` + `CreateNewsPostDto.content`; DTO update kế thừa qua `PartialType`. **Không** `extends TranslatedTextDto` vì class-validator kế thừa cả decorator, `@MaxLength(5000)` của lớp cha sẽ vẫn chặn.
  - **Field ngắn giữ nguyên 5.000** — `title`, `summary`, `slug`, `name`, `caption`, `location`, `category`, banner… vẫn dùng `TranslatedTextDto`. Có test khẳng định chúng không bị nâng lây.
  - **Không cần migration** — cột `content` của `news_posts` và `pages` là **JSONB** (`20260707093428_init`), không có ràng buộc độ dài; toàn bộ migration không có `VARCHAR(n)` nào. `prisma validate` xanh.
  - **Trần body JSON 100 kb → 2 MB** (`common/body-limit.ts`, `main.ts` `useBodyParser`) — mặc định Express là 100 kb ≈ 80.000 ký tự tiếng Việt, thấp hơn trần DTO mới, nên phần vừa nới ra sẽ bị **413 trước khi** ValidationPipe chạy. (Bài ~5.000 từ của ticket gốc chỉ ~42 kb nên vốn đã lọt — đây là chỗ hở lộ ra khi nâng trần, không phải nguyên nhân lỗi 400.)
  - **Admin** — thêm `lib/long-form-content.ts`: trần khớp backend, validate **theo từng đoạn** (đúng thứ backend soi) chứ không theo tổng cả ô, báo lỗi tại chỗ thay vì đợi 400. Gom `splitParagraphs`/`paragraphsToText`/`toParagraphPayload` đang lặp ở `NewsFormDialog` + `PageFormDialog` về một chỗ. Ô nhập không đặt `maxLength` nên trình soạn thảo không cắt chữ.
  - **Test** — backend 38 test mới (`long-translated-text.dto.spec.ts`: 5.001 / 50.000 / đúng 100.000 chấp nhận, 100.001 chặn, cả `vi` lẫn `en`, cả 4 DTO create/update, báo đúng `content.1.vi`) + 5 test trần body. Admin 20 test (`long-form-content.test.ts`), gồm round-trip khẳng định payload gửi đi và form nạp lại **đủ nguyên văn**, không truncate.
  - **Kiểm định** — Backend: `prisma validate` xanh · `prisma generate` xanh · `jest 349/349` · `tsc` sạch · `lint` 0 lỗi (8 warning cũ, không thuộc thay đổi này) · `build` xanh. Admin: `vitest 146/146` · `tsc` sạch · `lint` 0 lỗi (1 warning cũ ở `ui/form.tsx`) · `build` xanh.
  - **Ghi chú** — index GIN `news_posts_search_idx` tính `to_tsvector` trên cả `content`; `tsvector` có trần cứng 1 MB của PostgreSQL. Chỉ chạm tới khi một bài vượt ~1 triệu ký tự (≥10 đoạn kịch trần) — xa hơn nhu cầu thực tế nhiều lần, chưa xử lý, ghi lại để biết.

### Phiên 2026-07-29 — Sửa CI E2E đỏ (E2E-CI-RED-FIX-M1)

- [x] **E2E-CI-RED-FIX-M1** — CI `e2e-fullstack.yml` cho **109 passed · 1 failed · 2 flaky**; sau bản sửa: **113/113, 0 fail, 0 flaky** (chạy lại 2 lượt, một lượt `--retries=0`). Báo cáo đầy đủ: [2026-07-29-e2e-ci-red-contrast-overflow-flake-fix.md](../08-audits-and-reports/current/2026-07-29-e2e-ci-red-contrast-overflow-flake-fix.md).
  - **Tương phản huy hiệu** — cặp `green-700`/`green-50` chỉ **4.723:1** (sát ngưỡng 4.5), lại bị hiệu ứng `.row-in` (mờ dần opacity hàng bảng) làm axe đo ra 3.86–4.47:1 → đỏ chập chờn. Thêm token ngữ nghĩa `--color-status-*` ở `admin/src/index.css`, `badge.tsx` + `StatCard.tsx` dùng token, bỏ `transition-colors` khỏi nhãn tĩnh. Success **7.80:1**, viền ≥ 3:1 so với nền huy hiệu / thẻ trắng / `canvas`. Thêm test hồi quy đo đúng huy hiệu “Đang hoạt động” (đòi ≥ 6:1, viền ≥ 3:1, cỡ chữ < 18.66px, `transition-duration: 0s`).
  - **Tràn ngang mobile** (`scrollWidth 388 > innerWidth 375`) — email `dautuxaydungthienduc@yahoo.com` là từ liền không ngắt được, đặt “sàn” min-content ~344px cho khối `CtaContactCards`, trong khi hộp nội dung ở 375px chỉ có 343px → trang **đã tràn sẵn 1px**, vừa lọt dung sai ±1px; font runner CI rộng hơn thì thành 388px. Sửa bằng `min-w-0` + `wrap-anywhere` trong `cta-contact-cards.tsx` (`wrap-break-word` **không đủ** — không giảm min-content). Hết tràn ở mọi bề rộng 320–420px.
  - **Chập chờn bàn phím banner** — test bấm phím ngay khi `h1` (do server render) hiện, **trước** khi React gắn `onKeyDown` → phím bị nuốt. Thêm cổng hydrate (`.banner-progress` biến mất dưới `prefers-reduced-motion`), khẳng định focus đúng nút, và chờ **đổi chỉ số slide** qua `aria-current` trước khi kiểm nội dung. 10/10 lượt xanh.
  - **Ổn định axe** — `e2e/helpers/a11y.ts` đưa trang về trạng thái hình ảnh cuối trước khi quét (bật reduced-motion — chế độ CSS có sẵn của sản phẩm — rồi tua animation đổi màu về đích và chờ ổn định theo điều kiện). Nhờ vậy lộ ra vi phạm có sẵn `text-gold` trên `bg-brand` = **3.53:1**; đổi sang token sẵn có `text-gold-soft` = **4.84:1** ở cả 6 chỗ dùng chung idiom. **Không tắt rule, không loại trừ phần tử, không tăng timeout, không thêm sleep.**
  - **Chẩn đoán tràn ngang dùng chung** — helper mới `e2e/helpers/layout.ts`: giữ nguyên ngưỡng, chờ font + bố cục ổn định rồi mới đo, và khi đỏ thì in đúng phần tử gây tràn (selector, rect, width/min-width, margin, transform, position, tổ tiên cắt tràn).
  - **Kiểm định**: Admin `vitest 126/126` · coverage PASS · `tsc` sạch · `lint` 0 lỗi (1 warning cũ ở `ui/form.tsx`, không thuộc thay đổi này) · `build` xanh. Frontend `jest 53/53` · `tsc` sạch · `lint` 0 lỗi/0 warning · `next build` xanh. Backend không đổi file nào.

### Phiên 2026-07-28 — Nội dung banner trang chủ (THIEN-DUC-BANNER-CONTENT-IMPLEMENTATION-M1)

- [x] **THIEN-DUC-BANNER-CONTENT-IMPLEMENTATION-M1** — điền nội dung banner trang chủ song ngữ cho **4 ảnh banner đã có sẵn**. Bảng `banners` trước đó **rỗng** nên khối hero trang chủ không render gì (`HomeBannerSlider` trả `null` khi không có banner). Không tạo ảnh mới, không upload lại ảnh cũ, không đổi schema, không migration.
  - **Nguồn sự thật nội dung**: `backend/prisma/banner-content.json` (4 bản ghi) — dùng chung cho seed, cho test backend và cho E2E, nên ba nơi không thể lệch nhau. Nạp bằng `backend/prisma/seed-banners.js` (`npm run prisma:seed:banners`).
  - **Idempotent theo khóa nghiệp vụ `image`**: có bản ghi cùng ảnh → `UPDATE` **giữ nguyên `id`**; chưa có → `INSERT`. Không `DELETE`, không đụng banner do Admin tự thêm. Chạy lại nhiều lần vẫn đúng 4 bản ghi / 4 ảnh phân biệt (đã kiểm). Cầu chì `SEED_CONFIRM_PRODUCTION` như các seed khác.
  - **Ánh xạ ảnh → nội dung** (thứ tự 0→3, tất cả `isActive`): master-plan-02 → *Khu đô thị Hưng Phú, TP. Bến Tre* (`/du-an/khu-do-thi-hung-phu`, `objectPosition 35% center`); aerial-01 → *Fancy Tower — 196 căn hộ tại Hưng Phú* (`/du-an/khu-do-thi-hung-phu/fancy-tower`, `45% center`); fancy-tower-01 → *Dự án Thiên Đức tại các tỉnh phía Nam* (`/du-an`, `center center`); master-plan-top-01 → *Thiên Đức — xây dựng từ năm 2010* (`/gioi-thieu`, `center center`). **Tên file lệch nội dung ảnh** (ảnh tên `fancy-tower-01` là phối cảnh tổng thể, ảnh tên `aerial-01` mới là tòa căn hộ) — ánh xạ dựa trên ảnh thật, không dựa trên tên file.
  - **Dữ kiện dùng trong nội dung đều đã được công ty xác nhận** (`01-requirements/open-questions.md` §1 câu 2 và 7): 11,25 ha · Nguyễn Thị Định, Phú Tân, TP. Bến Tre · 330 căn thấp tầng · Fancy Tower 19 tầng nổi + 1 hầm, 196 căn, đã nghiệm thu & bàn giao · thành lập 2010 · hợp tác CapitaLand tại TP.HCM · giá trị cốt lõi. **Không** thêm giải thưởng/số liệu/mốc thời gian ngoài danh sách đó.
  - **Trần độ dài đo từ chính khung hero**, không phải con số tùy chọn: tiêu đề ≤ 45 ký tự (`line-clamp-2`, tới `3.25rem` ở desktop) và mô tả ≤ 125 ký tự (`line-clamp-3`, `text-sm` ở mobile 375px). Bản nháp đầu 52–55 ký tự tiêu đề **bị clamp mất chữ ở tablet + desktop** — phát hiện bằng E2E responsive rồi rút gọn.
  - **Frontend**: gỡ mảng `homeBanners` chép cứng trong `src/data/banners.ts` (không nơi nào import, trùng nội dung với backend → nguồn sự thật thứ hai). Giữ lại kiểu `HomeBanner`.
  - **Test mới**: backend `src/banners/banner-content.spec.ts` (8 test — validate qua đúng `CreateBannerDto`, song ngữ đủ, độ dài, `href` đối chiếu route tĩnh + slug trong `seed-projects.js`, `objectPosition`, thứ tự); frontend `src/lib/api/banners.test.ts` (4 test — `mapBanner` VI/EN + fallback); admin `src/pages/BannersPage.test.tsx` (6 test — danh sách, ảnh xem trước, bật/tắt, form nạp VI/EN + payload khớp DTO, validate `href`); E2E `admin/e2e/public/banner-content.e2e.ts` (**21 test** — API công khai chỉ lộ banner bật, trang chủ VI/EN, ảnh tải được + alt, bàn phím, route CTA mở được ở cả hai locale, responsive 375/768/1280, axe khối banner, `prefers-reduced-motion`).
  - Helper `admin/e2e/helpers/a11y.ts` thêm tham số `include` **tùy chọn** để giới hạn phạm vi quét axe — **không tắt/hạ mức rule nào**.
  - **Kiểm định**: Backend `prisma validate` · `tsc` · `lint` (0 lỗi) · `jest 306/306` · `build` xanh. Admin `tsc` · `lint` (0 lỗi) · `vitest 126/126` · `build` xanh. Frontend `tsc` · `lint` (0 lỗi/0 warning) · `jest 53/53` · `next build` xanh (prerender cả `/vi/du-an/khu-do-thi-hung-phu/fancy-tower` → route CTA có thật). E2E full-stack **111/112** cục bộ — nhưng lần chạy **CI** thật của cùng bộ test cho **109 passed · 1 failed · 2 flaky**; xem đính chính ở mục E2E-CI-RED-FIX-M1 bên dưới.
  - **Tồn đọng — KHÔNG do banner**: (a) `admin/e2e/admin/accessibility.e2e.ts` "Admin quản lý tài khoản: axe" fail `color-contrast` (badge `text-green-700` trên `bg-green-50` = 4.3:1; `text-slate` #7a8287 trên trắng = 3.91:1) — **đã xác minh có sẵn trước thay đổi này** (stash helper a11y, chạy lại, vẫn fail); (b) trang chủ ngoài khối banner còn 1 vi phạm `color-contrast` ở CTA liên hệ (`text-gold` trên `bg-brand` = 3.52:1). ➡️ **Cả hai đã đóng ngày 2026-07-29** ở E2E-CI-RED-FIX-M1.
  - **Chưa chạy trên production** — seed mới chỉ chạy trên DB test cục bộ `localhost/thien_duc_test`. Muốn lên production phải chạy `SEED_CONFIRM_PRODUCTION=yes npm run prisma:seed:banners` có chủ ý.

### Phiên gần đây (2026-07-20 → 21)

- [x] **PROJECT-GALLERY-IMAGES-FIX-M1** — bố cục gallery+hạng mục có điều kiện (`showGalleryUnderMain = items>0 && gallery>0`: ảnh chính → `ProjectPhotoStrip` gallery cấp dự án → hạng mục); gallery ưu tiên `galleryImages` **chỉ ảnh cấp dự án** (`projectItemId null`), fallback `gallery` phẳng; thêm `mapProjectGallery` + test. **Kiểm chứng thủ công 2026-07-21: PASS** (3 case, không render trùng, ảnh cấp dự án, mobile OK). Commit `8cf7d0f`.
- [x] **PROJECT-IMAGES-UI-FIX-M1** — hiển thị ảnh Cloudinary production: `next.config.ts` khai `images.remotePatterns` cho `res.cloudinary.com` + CSP `img-src`; `next.config.spec.ts` cập nhật. Commit `2075d36`, `adee50b`.
- [x] **ADMIN-SUPER-ADMIN-GLOBAL-APPROVAL-BYPASS-M1** — helper `backend/src/common/content-approval.ts` (`canBypassApproval`/`initialContentStatus`): `SUPER_ADMIN` tạo nội dung `PUBLISHED` ngay, `ADMIN`/`EDITOR` vào `DRAFT`; nối projects/news/pages/cooperation + test. Commit backend `ee254af`, admin `aa32059`.
- [x] **ADMIN-CONTENT-STATUS-WORKFLOW-CONSISTENCY-M1** — chuẩn hóa luồng đổi trạng thái đồng nhất 4 module (News/Projects/Pages/Cooperation). Backend: thêm `assertContentStatusTransition` (chốt mịn sau `RolesGuard`) — `EDITOR` chỉ được gửi duyệt `DRAFT→PENDING`, `ADMIN`/`SUPER_ADMIN` đặt trạng thái tùy ý; route `.../status` mở cho `EDITOR` nhưng service chặn 403 nếu vượt quyền; 4 service nhận `actorRole`. Admin: cả 4 module dùng chung `contentStatusActions` (bỏ gate `canApprove` ở Projects, thay toggle DRAFT↔PUBLISHED ở Pages/Cooperation bằng bậc thang). Sửa News hết lỗi nút "Gửi duyệt" của EDITOR trả 403. Test backend (helper + News updateStatus theo vai trò) + admin helper. **Chưa kiểm chứng thủ công.**
- [x] **ADMIN-CONTENT-WORKFLOW-BUSINESS-RULE-AUDIT-M1** — **Quy tắc nghiệp vụ chốt cuối: Option B** (công ty nhỏ, ít quản trị viên). Áp dụng đồng nhất cho **News · Projects · Pages · Cooperation**:
  - **EDITOR**: `DRAFT → PENDING` (nút "Gửi duyệt") — **không** được đăng thẳng.
  - **ADMIN**: `DRAFT → PUBLISHED` đăng thẳng (nút "Đăng ngay"); `PENDING → PUBLISHED` (nút "Duyệt & đăng"); `PENDING/PUBLISHED → DRAFT` (nút "Trả về nháp"); duyệt được nội dung EDITOR gửi lên — **không** phải tự gửi duyệt bản nháp của mình.
  - **SUPER_ADMIN**: toàn quyền luồng nội dung — `DRAFT → PUBLISHED` đăng thẳng, duyệt/trả về nháp; `create` vẫn xuất bản ngay ở nơi có bypass.

  Ghi chú triển khai:
  - **Backend runtime đã cho phép sẵn `ADMIN DRAFT → PUBLISHED`** (`assertContentStatusTransition` để ADMIN/SUPER_ADMIN đặt trạng thái tùy ý) — Option B **không thêm thay đổi runtime backend nào**.
  - Option B chỉ cần **cập nhật helper UI admin** `content-status-actions.ts`: nhánh `DRAFT` dùng `canApproveRole` (ADMIN+) → "Đăng ngay" thay vì "Gửi duyệt"; gỡ `canBypassRole` không còn dùng. Các trang module không đổi (đã render theo helper).
  - **Thay đổi backend chỉ là test** (`content-approval.spec.ts` thêm khẳng định `ADMIN DRAFT → PUBLISHED` không ném lỗi) để khóa hợp đồng; không đụng route/service.
  - Cập nhật test admin `content-status-actions.test.ts` (ADMIN nháp → "Đăng ngay"/PUBLISHED).
  - Kiểm định: Admin `lint`/`vitest 62`/`build` xanh; Backend `tsc`/`lint`/`jest 103`/`build` xanh.
  - **Redeploy admin: BẮT BUỘC** (đổi logic UI). **Redeploy backend: chỉ cần vì các thay đổi runtime backend của M1 vẫn chưa commit** — bản thân Option B không thêm runtime backend. **Chưa kiểm chứng thủ công.**
- [x] **IMPLEMENTATION-PLAN-CHECKBOX-AUDIT-M1** (2026-07-21) — kiểm đếm & làm sạch checkbox: xác định `[ ]` tăng 11→22 là do PLAN-RESTRUCTURE-M1 đổi văn xuôi→checkbox (không phải việc mới; git `c65787d`); giải trùng cặp placeholder; ghi nhận 3 việc phiên gần đây. Tiếp nối bằng **FINAL-RESTRUCTURE-M2** (2026-07-21) — gom toàn bộ về cấu trúc §0–§7 này.
- [x] **Email Resend-only closeout** (2026-07-20) — SMTP fallback gỡ khỏi code, `SMTP_*` gỡ khỏi Render, Gmail App Password thu hồi. Notes: [EMAIL-RESEND-CLOSEOUT](../08-audits-and-reports/current/2026-07-20-email-resend-closeout.md), [SMTP-REMOVAL-ENV-CLEANUP](../08-audits-and-reports/current/2026-07-20-smtp-removal-env-cleanup.md).
- [x] **FRONTEND-UI-POLISH-M1** — commit `85d5fb1` "Fix UI UX FRONT END" (Nhóm 3+4).

### Phiên 2026-07-24 — Quên mật khẩu, Admin UI + a11y (CMS-AUTH-FORGOT-PASSWORD-PHASE2)

- [x] **CMS-AUTH-FORGOT-PASSWORD-PHASE2-ADMIN-UI-UX-M1** — giao diện Admin cho luồng quên/đặt lại mật khẩu, nối 3 endpoint backend (PHASE1). Dùng skill `frontend-design` + `ui-ux-pro-max`; giữ nguyên hệ thiết kế Thiên Đức (nâu ấm/vàng, nền trắng, ảnh dự án, chữ Be Vietnam Pro + Inter). **Chỉ frontend Admin** — không đụng backend, không schema, không đăng ký công khai, không OTP.
  - **Trang đăng nhập**: thêm link phụ **"Quên mật khẩu?"** cùng hàng với "Ghi nhớ đăng nhập" → `/quen-mat-khau`; link thật (bàn phím + focus ring), không cạnh tranh nút "Đăng nhập". Giữ dòng "liên hệ Super Admin", không thêm link đăng ký.
  - **`/quen-mat-khau`** (công khai): nhập email → `requestPasswordReset` (`skipAuthHandler`) → màn hình xác nhận **trung tính** "Kiểm tra email của bạn" + nhắc hiệu lực 20 phút. **Không lộ email** đã nhập; chặn double-submit; lỗi mạng thật → toast chung, giữ form; nút **"Gửi lại"** (email giữ trong ref, không hiển thị; cooldown 60s của backend làm bấm lại thành no-op an toàn).
  - **`/dat-lai-mat-khau?token=…`** (công khai): states validating → invalid / form → success. Token đọc **một lần trong thân render** (chốt qua `useRef`), **xoá khỏi URL ngay** bằng `history.replaceState`, **không** lưu localStorage/sessionStorage/cookie/queryCache/global, **không** hiển thị/log. Form (PasswordInput, min 8 / max 128 / khớp, validate inline) → `resetPassword` payload đúng `{token,newPassword,confirmPassword}` → "thành công" + nút **"Đăng nhập"** (KHÔNG tự đăng nhập, không tự điều hướng). Phân biệt token chết (`ApiRequestError.status ≠ 0` → invalid) với mất mạng (`status 0` → toast, giữ form). Trạng thái invalid có "Yêu cầu liên kết mới" → `/quen-mat-khau`.
  - **`AuthLayout` dùng chung**: gom khối logo + cột biểu mẫu + panel ảnh dự án + quy tắc responsive cho **cả 4 màn hình** (Login, Account Setup, Forgot, Reset) — bỏ 4 khối brand + 2 panel ảnh trùng lặp. Split-screen ở `lg+`, một cột ưu tiên form (ẩn ảnh) ở mobile/tablet; CTA chính cao 44px; `min-h-dvh`.
  - **StrictMode-safe**: sửa mẫu đọc token của cả `ResetPasswordPage` và `AccountSetupPage` (đọc ở thân render, không dọn ref trong cleanup) để `<StrictMode>` gọi effect kép ở dev không làm mất token sau khi URL bị strip.
  - **A11y (PHASE2B)**: `CMS-AUTH-FORGOT-PASSWORD-PHASE2B-ADMIN-A11Y-FIX-M1` — nút hiện/ẩn mật khẩu (shared `PasswordInput`) **bỏ `tabIndex={-1}`** → vào được tab order, kích hoạt bằng Enter/Space (native `type="button"`), focus ring nâu đồng toàn cục (4.42:1), icon `aria-hidden`, `aria-label` đổi "Hiện/Ẩn mật khẩu". Không đổi giao diện, không đổi hành vi hiện/ẩn.
  - **Test**: +LoginPage (link), +ForgotPasswordPage (6), +ResetPasswordPage (12, gồm StrictMode + token không lộ/không lưu), +PasswordInput (6, bàn phím Tab/Enter/Space + không submit). Regression AccountSetup/Users/UserFormDialog/ProtectedRoute xanh. **Tổng vitest 120 xanh.**
  - Kiểm định: `tsc --noEmit`, `lint` (0 error, 1 warning cũ ở `ui/form.tsx`), `vitest 120`, `build` — tất cả xanh. **Chưa kiểm chứng thủ công** (cần dev server + luồng email thật).

### Phiên 2026-07-24 — Quên mật khẩu, backend (CMS-AUTH-FORGOT-PASSWORD)

- [x] **CMS-AUTH-FORGOT-PASSWORD-PHASE1-BACKEND-M1** — luồng quên mật khẩu phía backend, an toàn, dựa trên link email dùng một lần. Sau PHASE3C (không ai đặt mật khẩu hộ được), đây là lối tự phục hồi cho người dùng quên mật khẩu. **Chỉ backend** — không đụng Admin UI, không frontend công khai, không OTP/SMS, không đăng ký công khai, không retire `POST /users`.
  - **Data model**: bảng **riêng** `PasswordResetToken` (`password_reset_tokens`) — **KHÔNG** tái dùng `AccountInvitation`. Chỉ lưu `tokenHash` (SHA-256), không bao giờ lưu token gốc; `expiresAt`/`usedAt`/`revokedAt` cho vòng đời dùng-một-lần; `onDelete: Cascade` theo User. Migration mới `20260724100000_add_password_reset_tokens` (chỉ tạo bảng + index + FK, không đụng bảng khác). Thêm quan hệ `User.passwordResetTokens`.
  - **Endpoint công khai** (`auth.controller`): `POST /auth/forgot-password` (trả **trung tính** dù email tồn tại hay không), `POST /auth/validate-password-reset` (chỉ `{ valid: boolean }`, không lộ thông tin tài khoản), `POST /auth/reset-password` (lỗi chung cho mọi lý do token hỏng). DTO: `ForgotPasswordRequestDto`/`ValidatePasswordResetDto`/`ResetPasswordDto` — `whitelist + forbidNonWhitelisted`, min 8 / max 128, không nhận `email`/`role`/`userId`/`isActive`/`setupCompletedAt`/`passwordHash`/`tokenHash`.
  - **Token**: `generateOpaqueToken` (32 byte) + hash SHA-256; hết hạn **20 phút**; cooldown **60 giây/tài khoản**; yêu cầu mới **thu hồi** token còn hiệu lực cũ. Token gốc chỉ đi vào MailService trong bộ nhớ, không log/không trả HTTP.
  - **Neutral & không gửi mail** cho: email không tồn tại, `isActive = false`, `setupCompletedAt = null` (tài khoản chờ thiết lập dùng luồng lời mời), và khi dính cooldown.
  - **reset-password race-safe**: claim nguyên tử bằng `updateMany({ where: { usedAt: null, … } })` trong transaction — đúng một request thắng, còn lại nhận lỗi chung. Thành công: đánh dấu `usedAt`, cập nhật **DUY NHẤT** `passwordHash`, thu hồi **mọi refresh token** (kết thúc phiên), thu hồi các token đặt lại khác — tất cả trong cùng transaction (lỗi giữa chừng → rollback trọn). Không tự đăng nhập; **không** đổi role/email/hồ sơ/`setupCompletedAt`/`lockedUntil`.
  - **Email** (`MailService.sendPasswordResetEmail`, Resend): URL `${ADMIN_APP_URL}/dat-lai-mat-khau?token=…`; giữ quy ước **no-throw/no-op** (thiếu `ADMIN_APP_URL`/Resend → cảnh báo & bỏ qua an toàn); nội dung tiếng Việt, nhắc hết hạn 20 phút + dùng một lần + bỏ qua nếu không yêu cầu; escape giá trị nhúng; không log token/URL.
  - **Throttle**: forgot-password 5/15′, validate-password-reset 10/15′, reset-password 5/15′ (giữ nguyên throttle toàn cục).
  - **Test** (+60, tổng jest 277 xanh): 3 DTO spec; `auth.service` (neutral/cooldown/revoke/race/chỉ đổi passwordHash/thu hồi phiên/không auto-login/validate); `mail.service` (recipient/subject/URL từ `ADMIN_APP_URL`/token chỉ trong payload/no-op khi thiếu cấu hình/nuốt lỗi provider); `auth.controller` (3 endpoint + throttle). Regression login/invitation/refresh/logout/update-reject-password xanh.
  - Kiểm định: `prisma format`/`validate`/`generate`, `tsc --noEmit`, `lint` (0 error), `jest 277`, `build` — tất cả xanh. **Migration chưa apply** (local `DATABASE_URL` rỗng, không có Docker) → migration viết tay theo đúng mẫu `add_account_invitations`; cần `prisma migrate deploy` khi có DB. **Chưa kiểm chứng thủ công** (cần DB + Resend thật).
  - Việc còn lại → §2: **trang Admin quên/đặt lại mật khẩu** (frontend).

### Phiên 2026-07-23 — Luồng lời mời tài khoản (CMS-ACCOUNT-INVITATION)

- [x] **CMS-ACCOUNT-INVITATION-PHASE3C-REMOVE-ADMIN-PASSWORD-EDIT-M1** — gỡ nốt khả năng SUPER_ADMIN tự đặt mật khẩu cho tài khoản khác. Phase 3B mới bỏ ô mật khẩu ở nhánh **tạo mới**; nhánh **sửa** vẫn giữ ô "Mật khẩu mới" sau `{isEdit && …}`, và cả chuỗi phía sau còn sống (`UpdateUserInput.password` → `PATCH /users/:id` → `UpdateUserDto extends PartialType(CreateUserDto)` → `UsersService.update()` băm vào `passwordHash`).
  - **Admin**: `UserFormDialog` bỏ ô "Mật khẩu mới", state/validate/helper text "Để trống nếu không muốn đổi mật khẩu." và giá trị password khỏi payload submit; mô tả dialog đổi sang "…Nếu thay đổi vai trò, người dùng có thể cần đăng nhập lại.". Giữ nguyên name/email/role, khóa tự đổi vai trò (`isSelf`), toast, bố cục.
  - **Admin API**: `UpdateUserInput` bỏ field `password` → payload cập nhật **không thể** mang mật khẩu. `CreateAccountInvitationInput` không đổi.
  - **Backend**: `UpdateUserDto` chuyển sang `PartialType(OmitType(CreateUserDto, ['password']))` — `PATCH /users/:id` **không còn nhận** `password`, gửi kèm bị `ValidationPipe` (`whitelist + forbidNonWhitelisted`) trả **400**. `UsersService.update()` bỏ nhánh bcrypt và dựng `data` bằng **allow-list tường minh** (`name`, `email`, `role`, `isActive`) — hàng rào thứ hai, đúng cả khi gọi service trực tiếp không qua pipe. Thu hồi phiên nay chỉ khi đổi vai trò hoặc khóa tài khoản. Giữ nguyên chặn hạ quyền Super Admin cuối, tự đổi vai trò/tự khóa, và `passwordHash` không bao giờ lộ ra response.
  - **Không đụng**: luồng accept-invitation vẫn cho người được mời **tự đặt mật khẩu**; login (so khớp mật khẩu) không đổi; `CreateUserDto` giữ nguyên; **không đổi schema, không chạy migration**.
  - Test: Admin thêm 3 test chế độ sửa (không có ô mật khẩu, không có helper text, payload không chứa `password`); Backend thêm `update-user.dto.spec.ts` (chặn `password`/`passwordHash`/field nội bộ, vẫn nhận name/email/role/isActive) và thay test cũ "thu hồi phiên khi đặt lại mật khẩu" bằng test ép `password` vào dto → Prisma chỉ nhận `{ name }`.
  - Kiểm định: Admin `tsc`/`lint`/`vitest 94`/`build` xanh; Backend `prisma validate`/`tsc`/`lint`/`jest 217`/`build` xanh (lint warning còn lại đều là dòng cũ). `prisma generate` chạy lại do client sinh sẵn bị cũ — **không phải migration**. **Chưa kiểm chứng thủ công.**
  - Việc còn lại → §2: **Forgot Password** (chưa có — sau thay đổi này người dùng quên mật khẩu không còn lối tự phục hồi) và **retire `POST /users`** (đường tạo tài khoản kèm mật khẩu vẫn reachable ở backend, Admin UI không còn gọi).

### Phiên 2026-07-22 — Hardening phân quyền & bảo mật (trước production setup)

> Nguồn: **SECURITY-CODING-REMAINING-AUDIT-M1** (rà soát chỉ việc code/security còn
> mở trước production) + **ADMIN-ROLE-VISIBILITY-AUDIT-M1** (rà quyền hiển thị Admin CMS).
> Các fix dưới đây đều **backend/admin runtime → cần redeploy**; chưa kiểm chứng thủ công.

- [x] **ADMIN-ROLE-VISIBILITY-AUDIT-M1** — rà quyền sidebar/route/nút theo vai trò (EDITOR/ADMIN/SUPER_ADMIN) trên toàn Admin CMS. Kết luận: nav + route + nút đã khớp backend, không có nút nào gây 403; phát hiện 3 điểm cần siết → R1/R2/R3 dưới. Không đổi runtime ở bước audit.
  - [x] **R1 — Media delete chỉ ADMIN+** — xóa ảnh là thao tác phá hủy (gỡ Cloudinary, có thể hỏng trang đang dùng). Backend `media.controller.ts`: `DELETE /media/:id` thêm method-level `@Roles(ADMIN, SUPER_ADMIN)` (override EDITOR+ ở controller). Admin `MediaPage.tsx`: ẩn nút xóa với EDITOR (`canDelete`). Upload/list giữ nguyên cho EDITOR. Test: `media.controller.spec.ts` (metadata @Roles) + `MediaPage.test.tsx`.
  - [x] **R2 — Banner governance chỉ ADMIN+** — banner là nội dung trang chủ, không có luồng duyệt riêng. Backend `banners.controller.ts`: `create`/`update`(toggle `isActive`)/`reorder` chuyển sang `@Roles(ADMIN, SUPER_ADMIN)` (delete vốn đã ADMIN+). Admin: ẩn cả trang Banner khỏi EDITOR (nav `roles` + `ProtectedRoute` ở `/banner`), giống Liên hệ/Tài khoản. Test: `banners.controller.spec.ts` + `nav.test.ts`. EDITOR vào `/banner` trực tiếp → `/403`.
  - [x] **R3 — Users ADMIN read-only (lock-in test)** — hành vi đã đúng sẵn (ADMIN xem danh sách/chi tiết, mọi thao tác tạo/sửa/khóa/đổi vai trò gated `canManage = SUPER_ADMIN`; backend mutation `@Roles(SUPER_ADMIN)`); **không đổi runtime**, chỉ thêm test khóa hợp đồng: `users.controller.spec.ts` (đọc ADMIN+, mutation SUPER_ADMIN-only, self-service `/users/me` vẫn EDITOR+) + `UsersPage.test.tsx`.
- [x] **TRUSTED-PROXY-RATE-LIMIT-FIX-M1** (Finding 7) — sau reverse proxy của Render, `req.ip` phải lấy từ `X-Forwarded-For` thì `@nestjs/throttler` (rate-limit contact/auth) và IP lưu kèm lead mới đúng IP client; nếu không, mọi request gộp chung IP proxy → chặn nhầm cả trang / rate-limit theo IP mất tác dụng. Fix backend: `main.ts` tạo app kiểu `NestExpressApplication` + gọi helper `common/trust-proxy.ts` (`configureTrustProxy` → `app.set('trust proxy', 1)` — tin đúng **1 hop** Render, **không** `true` để tránh giả `X-Forwarded-For`). Test `trust-proxy.spec.ts`. Xác minh X-Forwarded-For thật là **manual trên staging**.
- [x] **ADMIN-SPA-SECURITY-HEADERS-M1** — Admin (Vite static trên Vercel) trước đây **không có** header bảo mật như FE public. Thêm `admin/vercel.json`: `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Strict-Transport-Security: max-age=31536000; includeSubDomains`, `Permissions-Policy: camera=(), microphone=(), geolocation=()`; áp cho mọi route `/(.*)` + SPA fallback rewrite `→ /index.html`. **CSP cố ý hoãn** (Radix/Tailwind/Sentry có inline style + ingest, cần giai đoạn Report-Only riêng — xem →6). Test `vercel-headers.test.ts`. Redeploy Vercel mới có hiệu lực.

### Audit-baseline →1…→13 (2026-07-16 → 19)

> Nguồn: [audit-baseline](../08-audits-and-reports/current/2026-07-16-audit-baseline.md). Điểm khởi đầu 60/100.

- [x] **→1 Email báo lead** — `backend/src/mail/` (Nodemailer→Resend), lead lưu trước, gửi fire-and-forget nuốt lỗi, body HTML escape; 4 test. Production Resend PASS.
- [x] **→2 Rời free-tier + backup DB** (phần repo) — `render.yaml` plan trả phí; runbook backup-restore / rollback / deployment-guide 1b. *Thao tác Dashboard → §3.*
- [x] **→3 `@MaxLength` cho DTO chữ** — phủ 13 DTO (name 120/phone 30/email 200/message 5000/`TranslatedTextDto` 5000…), FE khớp `maxLength`; test DoS payload. (Finding #9) · **Sửa 2026-07-29:** nội dung dài (`content[]` của tin tức + trang) tách sang `LongTranslatedTextDto` trần **100.000** ký tự/đoạn — 5.000 chặn nhầm bài viết hợp lệ. Field ngắn giữ nguyên 5.000. Xem LONG-FORM-CONTENT-LIMIT-M1 (§7).
- [x] **→4 Bản dịch EN** — HOÀN TẤT phạm vi production/audited: CMS batch 1–4 + i18n UI tĩnh (B-series) + loạt C data-model song ngữ + [EN-FULL 7-route](../08-audits-and-reports/current/2026-07-18-en-full-group2-closure.md) + [EN-SITE-WIDE 5-route](../08-audits-and-reports/current/2026-07-18-en-site-wide-follow-up.md) + [EN-PROJECT-ITEMS-P1](../08-audits-and-reports/current/2026-07-18-en-project-items-p1.md) + [ADMIN-ITEM-CONTENT-P2 A–F](../08-audits-and-reports/current/2026-07-19-admin-item-content-p2-batch-m1.md). VI byte-identical, 0 `[object Object]`. Dự án tương lai nhập EN trước publish → **§5**. (câu 19)
- [x] **→5 Error tracking + uptime** (phần repo) — Sentry 3 app errors-only (no-op khi thiếu DSN, `beforeSend` xóa body/IP); doc `monitoring-and-alerting.md`. *DSN + UptimeRobot → §3.*
- [x] **→7 JSON-LD** — `Organization` toàn site + `NewsArticle` (tin) + `BreadcrumbList`; builders trong `lib/seo.ts`. Cố ý chưa: `RealEstateListing`/`LocalBusiness`/`sameAs` (→ §6).
- [x] **→8 CI admin + test FE/e2e** — admin `ci.yml`; FE Jest 5 suite/36 test; backend e2e smoke + CI job Postgres; viết lại `next.config.spec.ts`/`app.e2e-spec.ts`. Doc `testing-strategy.md`. Bổ sung **SYS-P1-ADMIN-ROBUSTNESS** (Vitest admin, 53 test/12 file — [note](../08-audits-and-reports/current/2026-07-19-sys-p1-admin-robustness.md)).
- [x] **→11 Seed `gioi-thieu`/`lien-he`** — `prisma/seed-pages.js` (idempotent, `ON CONFLICT DO NOTHING`); chạy production, vòng đời CMS→API→DB→web xác nhận. Tiền đề của →4.
- [x] **→12 Bỏ cast `as unknown as Prisma.*Input`** — gỡ 14 cast/5 service, helper `common/prisma-json.ts` + `satisfies`; hành vi runtime giữ nguyên. [note](../08-audits-and-reports/current/2026-07-19-prisma-input-casts-removed.md).
- [x] **→13 G4 baseline production** — Rich Results 7/7 hợp lệ; Lighthouse CLI (Desktop 98–100, Mobile 77–91); fix NO_FCP/NO_LCP (`.page-transition` opacity + `prefers-reduced-motion`), đo lại 14/14 hợp lệ. Chi tiết [g4-measurement-baseline](../06-testing/g4-measurement-baseline.md). Còn lại → §6.
- *(→6 Enforce CSP: OPEN → §2 · →9 Auth HttpOnly + →10 Staging/WAF: deferred → §4)*

### Security Audit Phase 1–3 (2026-07-14)

> Chi tiết: [security-audit-phase-1](../05-security/security-audit-phase-1.md) + [security README](../05-security/README.md).

- [x] **Phase 1** — audit codebase, xác định 3 HIGH (CORS, rate-limit, SQL-inj), kế hoạch 4 phase.
- [x] **SEC-CORS-001** — bỏ fallback `?? '*'`, throw khi thiếu `CORS_ORIGIN`; 5 test.
- [x] **SEC-RATE-001** — `@Throttle` login 5/refresh 10/logout 20 per phút; 10 test.
- [x] **SEC-INJ-001** — `websearch_to_tsquery` → `plainto_tsquery` (không parse operator); 16 test.
- [x] **Phase 3 HTTP headers** — FE CSP/HSTS/X-Frame-Options; BE Helmet active. (CSP enforce còn lại → §2 →6)

### Sprint 0–4 — nền tảng & tính năng (2026-07-10 → 11)

- [x] **Sprint 0 — nền móng** — repo backend (NestJS+Prisma+Postgres, module theo domain); schema 12 bảng (field song ngữ JSON `{vi,en?}`); response envelope chuẩn; Swagger `/api/docs`; `.env.example`; CI lint+build. Hosting: Vercel + Render (câu 11).
- [x] **Sprint 1 — Auth + Projects** — `auth` (JWT access15′/refresh30d SHA-256 xoay vòng, khóa 423 sau 5 sai; 14 test), `users` (CRUD role, thu hồi token khi khóa/đổi quyền; 18 test), `projects` (CRUD + items + gallery + duyệt), route FE `du-an/[slug]/[hang-muc]`, lớp `lib/api/*`. Sửa enum `ProjectStatus` admin. (ED-01/03, KB-10)
- [x] **Sprint 2 — News + Cloudinary** — `news` (luồng nháp→duyệt→đăng, vá lộ DRAFT, 409 slug trùng), `media` (Cloudinary WebP≤1200px, chống path-escape, xóa cloud-trước), `pages`, `banners` (reorder transaction), seed news (1 bài thật), Admin nối API thật. Fix `sslmode=require`. (ED-04/05/06/07)
- [x] **Sprint 3 — Admin CMS + Contact + Email** — Dashboard (số liệu suy từ query), login API thật + ProtectedRoute + /403, màn duyệt nội dung, `contact` (`POST /contact` + rate-limit + email), quản lý lead, contact-form bỏ `mailto`. (ED-02, KB-06/08, YC-09)
- [x] **Sprint 4 — i18n + Search + SEO + nối API** — `[locale]` routing (VI không tiền tố, EN `/en`, Next.js 16 `proxy.ts`); JSONB song ngữ + fallback `.vi`; Admin nhập EN (`BilingualField`, `BannerFormDialog`/`PageFormDialog` mới); search full-text `ts_rank` (YC-10); SEO canonical/hreflang/sitemap/robots (YC-12); scheduler ED-08 idempotent; nối nốt FE↔API. 3 cấp role (câu 18). Vá lộ DRAFT projects, fix `@IsObject`→array, fix mất bản dịch khi PATCH item (**ADMIN-ITEM-CONTENT-P2 M1** A–F PASS, `8a9de74`).

### Nội dung thật + UI/UX + hợp tác (2026-07-10 → 16)

- [x] **Nội dung câu 2/6/7/8** — số liệu 4 dự án (đổi tên Silver Sea/Bảy Hiền Tower, giữ slug; lược nội dung pháp lý nhạy cảm khỏi web công khai); công ty thành viên; giới thiệu (timeline/stats); pháp lý (MST `0309910290` ở footer).
- [x] **Kho ảnh + gallery dự án** — 30 ảnh vào `images/` + `sync:images` (hash-based); bỏ bảng "Dự án tiêu biểu"→thẻ; slider ảnh mọi dự án. Fix dữ liệu seed (`location` ngắn, Bảy Hiền `DA_BAN_GIAO`, bỏ câu pháp lý nhạy cảm).
- [x] **Tinh chỉnh UI/UX dự án (Đợt 1–3)** — cân 2 panel Thông tin/Tổng quan, carousel hạng mục (bỏ hiện 2 lần), bố cục 2 cột trang hạng mục, nhúng Google Maps mọi dự án, `ProjectPhotoStrip` scroll-snap, footer gọn, cơ cấu lại trang chủ↔giới thiệu chống trùng lặp.
- [x] **Dự án hợp tác quản lý từ CMS (Đợt 3b)** — model `CooperationProject` + module `cooperation` (route công khai chỉ `PUBLISHED`), Admin `/du-an-hop-tac` (RHF+Zod), FE `getCooperationProjects()`. Migration `20260711120000_add_cooperation_projects` khi đó **chưa áp production**; nay **đã áp** cùng toàn bộ 11 migration → **§3** (COMPLETE).
- [x] **Redesign typography** — Geist → Playfair Display (H1/H2) + Be Vietnam Pro (body), subsets latin+vietnamese, design tokens `clamp()`, chuẩn hóa eyebrow, chống cắt dấu tiếng Việt.

### Hiện trạng kỹ thuật (tham chiếu)

- Deploy production: FE→Vercel, BE+Postgres→Render (`render.yaml`). Form liên hệ chạy thật end-to-end (`POST /api/contact` 201).
- Backend NestJS 11 + Prisma 7 + Postgres 17 (local Docker port **5433**). Admin CMS nối API thật 100% (`src/data/` mock đã xóa). `tsc`+`eslint` sạch, CI ở cả 3 repo.
- ⚠️ `.env` dev trỏ `DATABASE_URL` vào Render — trước go-live phải `prisma migrate deploy` có chủ ý (§3). Render free ngủ sau 15′, Postgres free hết hạn 90 ngày. Thời gian lưu UTC, hiển thị quy đổi VN (UTC+7) qua `formatDateTime`.
